---
description: "Use after /plan when a plan file exists in .forge/plans/ and you need to find weaknesses before implementation starts. Phase 3 of the forge dev pipeline. Spawns 4 persona-subagents in parallel (Skeptic, Pragmatist, Architect, User Advocate) that tear apart the plan from independent angles, then synthesizes their findings + writes the execution strategy (what goes to subagents, what runs in parallel, what's sequential) into the final plan. Hands off to /execute (Phase 4)."
disable-model-invocation: true
---

Invoke the forge:critique skill and follow it exactly.
