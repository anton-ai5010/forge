---
description: "Use after /new-task when a clean task + completion criteria exist and you need to build the implementation plan. Phase 2 of the forge dev pipeline. Researches the code and libraries silently, asks the user only about business logic (not technical details), recursively offloads distant blockers to separate sessions to keep main context clean, and outputs a plan with mandatory checkpoints — natural pauses on meaningful milestones, not every N steps. Hands off to /critique (Phase 3)."
disable-model-invocation: true
---

Invoke the forge:plan skill and follow it exactly.
