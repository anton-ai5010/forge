---
description: "Use after /new-task when a clean task exists in .forge/tasks/ and you want to reality-check the idea itself before planning. Phase 1.5 of the forge dev pipeline. Mirrors back how the idea was understood, then challenges it against the project's real context (decisions, dead-ends, status, existing code) — right problem? simpler path? what breaks? worth the effort? — offering a concrete better alternative for each weakness instead of just criticizing. A dialogue, not a report. Hands off to /plan (Phase 2)."
disable-model-invocation: true
---

Invoke the forge:refine-idea skill and follow it exactly.
