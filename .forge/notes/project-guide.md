- Шаг про SKILL.md: писать через skill-creator (skill-creator:skill-creator) + правила forge:writing-skills; не руками. Требование владельца 2026-09-06.

## Отклонения
- Шаги 4 и 5 (render.py) → один субагент вместо двух: тот же файл, та же спецификация (тест), второй агент перечитывал бы те же 80 КБ дизайна. Путь тот же — тест-спека, порядок функций из design-renderer.md.
- render.py: `render <json>` без out.html — раньше «третий режим» (публиковал в docs/guide без built_at); теперь: json = последняя версия → боевой путь, иначе отказ «макет — render <json> <out.html>». Ревью нашло, план молчал.
- render.py: `merged`/`link` на несобранной версии (built_at null) — как verdict: только JSON + «HTML не перерисован». План говорил это только про verdict; иначе недособранная версия затирала бы guide-latest.html.
- render.py: `cd_repo_root` — условие «есть `.forge/`» вместо «есть `.forge/guide` или легаси-json» (design-renderer.md:356): иначе первый bump во вложенном проекте без своего .git писал бы в корень чужого репо.
- render.py: валидный JSON не той формы (null, [], findings: null) → GuideBroken/пустые списки, catch-all в main → exit 0 со строкой «⚠ гайд не обновлён — ошибка рендерера». План покрывал только битый JSON.
- render.py: подпись под tg-диалогом — длинная форма из design-skill.md («Воспроизведение сообщений бота, данные вымышленные»), не короткая из стыков; тест ловит общую часть.
- render.py: `.changes` печатается только при непустом changelog[0].items (пустой список давал бы заголовок без содержимого).
- Чекпоинт A: владелец попросил ограничить ширину на экране → `@media screen{body{max-width:960px;margin:0 auto;padding:…}}` в CSS рендерера; печать/PDF без изменений (эталон Vespera без обёртки).
- Шаг 8: агент хуков дописал в план (строка 138) статус `dropped` в перечень verdict-статусов хука — синхронизация с рендерером, не смысловое изменение.
- Шаг 7 (триггеры): run_loop.py skill-creator — лучшим осталось исходное описание (точность 100%, recall низкий в обвязке claude -p). Владелец 2026-09-06: «на какие фразы срабатывает — не важно, буду указывать на фазу 5». Описание не меняем, дальше не оптимизируем.
- 2026-09-07: синтез v1.0 (workflow) был убит прерыванием владельца вечером 06.09 — перезапущен в облегчённом виде (синтезатор → 1 ревьюер → починка, без скептиков). Шаги 9–10 (документация, evals, трей, формат-док) запущены параллельно с синтезом, ДО чекпоинта B — они от его исхода не зависят; чекпоинт B по-прежнему до мержа.
