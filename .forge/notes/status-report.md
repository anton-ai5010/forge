# Заметки по задаче status-report

## Отклонения
- План ставил шаги 11–12 (навигатор/evals/документация) после Чекпоинта B. Сделаны параллельно с волной 1 (шаги 4–10): файлы не пересекаются, а B проверяет скилл, не документацию — экономия времени. B по-прежнему до релиза.
- Проверка (14) из плана разбита на три отдельных check в test-status-report.sh (создание, идемпотентная дописка, макет вне .forge) — 16 проверок вместо 14, суть та же.
- Чекпоинт A: макет отправлен владельцу картинкой ещё на планировании; ответ не дождались, работа продолжена — правки вида (CSS/BLOCKS в render.py) применимы в любой момент.
- Раннер prompt-тестов `tests/skill-triggering/run-test.sh` не работал на маке: `timeout` есть только в GNU coreutils, а `--output-format stream-json` в режиме `-p` требует `--verbose` (CLI отказывался стартовать). Без этого добавленные шагом 10 тесты нельзя было запустить вообще — починил минимально (fallback на `gtimeout`/без таймаута + флаг). Живой прогон после правки: ✅ PASS.
- Чекпоинт B прогнан Клодом на самом репозитории плагина (у него есть `.forge`), а не на отдельном проекте владельца: механика снимков проверена отдельно на локально поднятой странице (Playwright → PNG → data URI в отчёте, мусор убран).
