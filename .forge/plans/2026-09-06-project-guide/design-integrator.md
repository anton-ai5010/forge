# Интегратор: поправки к черновикам и плану (2026-09-06)

## Противоречия черновиков плану (что заменить)
- design-renderer.md, Шаг 2 (стр. 76–79, 258) и все проверки Шагов 3–9: имя теста `tests/hooks/test-guide.sh`
  => Заменить на `tests/hooks/test-project-guide.sh` (стык плана). Все команды `bash forge-plugin/tests/hooks/test-guide.sh …` → `…/test-project-guide.sh`.
- design-renderer.md, Шаг 3 (стр. 274–276): `FORGE_IGNORE` из 9 строк (…`status-report.html`, `reports/shots/`, `guide/shots/`) и `GUIDE_LINES = ("status-report.html", "reports/shots/", "guide/shots/")`
  => `FORGE_IGNORE` = 7 строк: `.inject-state .last-backup .migration-declined state.yml .github-* graph.json guide/shots/`; `GUIDE_LINES = ("guide/shots/",)`. Старые строки из шаблонов убираем.
- design-renderer.md, Шаг 2, проверка (12) стр. 213 (diff с 9 строками) и стр. 216, 219 (`wc -l … = "4"`)
  => diff — с 7 строками (без `status-report.html`/`reports/shots/`); ожидание `wc -l` → `"2"` в обоих местах (state.yml + только дописанный `guide/shots/`).
- design-renderer.md, Шаг 6 `do_bump` стр. 548–550 (перевод находок `done` прошлой версии в `deferred`) и пометка стр. 555
  => Эти 3 строки не писать — план Шаг 5 описывает bump без этого правила (сделанное остаётся `done`, зачёркнуто в конце яруса; в changelog уходит через pending_changes).
- design-renderer.md, Шаг 8 п.3 (стр. 716): backup.sh `git add .forge; [ -d docs/guide ] && git add docs/guide`, `--quiet -- .forge docs/guide`, `commit -- .forge docs/guide`; heredoc «после reports/shots/ добавить guide/shots/»
  => Брать вариант design-integration.md D4: массив `paths=(.forge)` + `if [ -d docs/guide ]; then git add docs/guide; paths+=(docs/guide); fi`, а `diff --cached`/`commit`/`reset` — по `"${paths[@]}"` (несуществующий pathspec после `--` роняет `git commit`). Heredoc — 7 строк, `guide/shots/` вместо двух старых.
- design-renderer.md, INTERFACES стр. 774 и design-skill.md INTERFACES стр. 538: statusline case `project-guide|status-report|"Phase 5"|5`
  => Case из стыков: `project-guide|guide|"Phase 5"|5) phase_icon="📖 Фаза 5: Гайд по проекту"` — без `status-report`.
- design-renderer.md, Шаг 1 (стр. 4) держит `status-report-sample.json` как LEGACY для теста (11); план Шаг 1 делает `git mv` фикстуры в `guide-sample.json`
  => Прав renderer: в Шаге 1 плана фикстуру НЕ переименовывать — `cp tests/hooks/fixtures/status-report-sample.json tests/hooks/fixtures/guide-sample.json` (старая остаётся как LEGACY для миграции). Проверка Шага 1 `grep -c '^R'` → 7, а не 8.
- design-skill.md, Шаг G1 п.4 (стр. 35–43) и проверки стр. 45: алиас `commands/status-report.md`; Шаг G7 стр. 511 исключение `commands/status-report.md` из свипа
  => Алиаса нет: `git mv commands/status-report.md commands/guide.md` (план Шаг 1). Из свипа G7 исключение убрать.
- design-skill.md, Шаг G1 п.3 (стр. 22–33): `commands/guide.md` с `argument-hint` и абзацем «If `$ARGUMENTS` is non-empty…»
  => guide.md — 3 строки по плану Шаг 1: description (текст из плана), `disable-model-invocation: true`, тело `Invoke the forge:project-guide skill and follow it exactly as presented to you`.
- design-skill.md, Шаг G2: description (стр. 57) — 1034 символа
  => Через skill-creator ужать до ≤1024 (резать EN-триггеры, не RU) — проверка Шага 7 плана это требует.
- design-skill.md, G2 (стр. 68–69, 95–96, 121–122, 137–141, 157) и G3 (стр. 278, 304–308, 313–315, 372, 384, 386, 403, 405), INTERFACES стр. 534–537, 539: файлы `.forge/guide/latest.json`, `.forge/guide/guide-vX.Y.json`, `.forge/guide/draft.json`, catalog `path: .forge/guide/latest.json`, фикстура `project-guide-sample.json`
  => Только `.forge/guide/vX.Y.json` (без префикса `guide-`); последняя = максимум по имени; черновика и живой копии нет — скилл правит последний `vX.Y.json` через Edit после `bump` (первый раз — после `init`). Catalog: `guide: {path: .forge/guide/, tags: [guide, report, what-next, findings, decisions-pending, owner, risks, versions]}`. Фикстура — `tests/hooks/fixtures/guide-sample.json`. Антипаттерн «писать в latest.json напрямую (только draft.json → build)» → «править старые `vX.Y.json`; новая версия — только через `bump`».
- design-skill.md, G2 стр. 79, 98, 385 и G3 стр. 347–352, 407, INTERFACES стр. 542–543: `render.py build` / `build --major` / `render` для latest
  => Режимы из стыков: первая сборка `init` → Edit JSON → `render` → `pdf`; следующая `bump` (или `bump major` на «это версия N») → Edit JSON → `render` → `pdf`; `open docs/guide/guide-latest.html`. Проверки G3 `grep -c 'render.py" build' ≥2` и `draft.json ≥4` заменить проверками плана Шаг 7 (`render.py" init|bump|pdf|verdicts`, `grep -c 'current.json\|latest.json\|draft.json\|status-report' → 0`).
- design-skill.md, G3 Шаг 8 таблица (стр. 378–386) и INTERFACES стр. 544: `render.py verdict A2 ok` / `verdict B2 redo "…"` / `verdict D2 discuss` / `verdict O1 answer "…"` / `verdict R4 up`, по одному verdict на код через `&&`; `redo → status accepted`; `discuss → 🔥 снят`; `O1 → находка done + answer`
  => Один вызов `render.py verdicts "<слова владельца целиком>"` — парсер сам разбирает все коды; статусы: ок → `accepted`, переделать: → `changed` (what новый, was старый), обсудить → `discuss` (🔥 остаётся), сделано → `works`; O-коды — это решения в `decisions[]` группы O (accepted/changed/discuss как остальные), не находки; R-коды: приоритет выше/ниже/отложить/ок. Переписывать `what` на «вы» после `changed` — через Edit последнего `vX.Y.json` + `render`.
- design-skill.md, G3 Шаг 4 (стр. 280–301) JSON-образец и INTERFACES стр. 548–550: схема `project/subtitle/audience/state_line/facts/changes/about.lead/note/shots[]/groups[]/glossary.def/tg_mocks.messages/plan.now[].text+codes`; O-коды живут в `findings` (owner decision|both) и не дублируются в `decisions`
  => Схема — design-renderer.md Шаг 1: `meta{project,version,project_version,date,audience,eyebrow,logo,built_from}`, `snapshot{facts[{lead,text}],howto_extra}`, `about{text,after,scheme[],screens[{file,caption,group}]}`, `parts[{icon,title,text,codes}]`, `flow`, `roles`, `decisions[{code,group,group_title,title,what,why,status,fire,since_version,source}]`, `findings` (скелет v1 + `links[]`), `plan{now,next,later}` — строки, `glossary[{term,text}]`, `tg_mocks[{title,bubbles[{who,text,buttons}]}]`, `changelog`, `pending_changes`. Группы — из `group`/`group_title` решений, отдельного `groups[]` нет; R-код = число из id находки для ВСЕХ находок; O-группа — только в `decisions[]`.
- design-skill.md, G3 Шаг 5а (стр. 328): снимки `.playwright-mcp/guide-<n>.png` → `.forge/reports/shots/guide-<n>.png`, запись в `shots[]`; Шаг 6 стр. 352 «`.forge/reports/shots/` — нет»
  => `.playwright-mcp/sr-<id>.png` → `.forge/guide/shots/<id>.png`; главный/ролевые экраны — `about.screens[{file,caption,group}]`, экран риска — `screenshot` у находки. Папка `.forge/guide/shots/` под игнором строкой `guide/shots/`.
- design-skill.md, G3 Шаг 6 (стр. 354–357) `git rm -q --cached .forge/status-report.json; rm -f …` внутри скилла; G2 стр. 96, 122 чтение старого `.forge/status-report.json` как прошлой версии
  => Миграцию делает `render.py init` (переносит находки и снимки, удаляет старые файлы) — в SKILL.md имя `status-report.json` не упоминать (проверка Шага 7: `grep -c status-report → 0`), говорить «старый отчёт „Что дальше“ — `init` перенесёт сам». `git rm .forge/status-report.json` — руками в Чекпоинте B.
- design-skill.md, G2 Секретарь (стр. 226): «в P всегда P1 и P2 «Кто утверждает что»»; G3 стр. 274 `sources.analysts — 7`, `state_line`, `subtitle`
  => Обязателен только P1 (его заводит `init`/скелет); P2 — по желанию Секретаря, если зон ответственности нет в памяти. Шапка — `meta.audience`, `meta.eyebrow`, `meta.project_version` (из index.yml, ставит `init`).
- design-skill.md, G4 (стр. 410–430): создание `prompts/product-mapping.txt`, `git rm prompts/status-report.txt`, в run-all две строки `product-mapping` + `project-guide`
  => План Шаг 1: `git mv prompts/status-report.txt prompts/project-guide.txt`, в run-all.sh:20 одна замена `"status-report"` → `"project-guide"`; негатив на Чекпоинте/Шаге 12 — `project-unblocker.txt` (есть). Текст промпта из G4 п.1 — взять (см. gaps).
- design-skill.md, G5 (стр. 433–477): `evals/criteria/project-guide.yml` (8 проверок), `phase: project-guide`, transition-matrix `to_project-guide`/`project-guide`, проверка `r[0][7]=='to_project-guide'`
  => Файл `evals/criteria/guide.yml` (git mv из status-report.yml), `phase: guide`, 9 проверок (7 старых переписать + `versions_incremental`, `codes_answered_in_chat`, `outsider_language_vy` — тексты G5 пригодны как формулировки), матрица `to_guide` / строка `guide`.
- design-skill.md, G6 (стр. 487, 496, 498, 500): в unblocker :246, new-task 9.5 и finishing — `.forge/guide/latest.json`
  => Условие наличия гайда — `ls .forge/guide/v*.json` (в тексте: «есть `.forge/guide/vX.Y.json`»); unblocker :246 — «последний `.forge/guide/vX.Y.json`».
- design-skill.md, INTERFACES стр. 546: «дефолты не ждут» в summary
  => Принято планом: ждут = decisions со status default|open|discuss + findings open с owner decision|both.
- design-integration.md, D2 (стр. 40, 47, 50, 58, 60), D3 (стр. 68, 78), D5 (стр. 129, 131, 141, 143), D7 (стр. 167–177), D8 (стр. 185–195, 201, 207), D12 (стр. 237–247), INTERFACES стр. 268, 274–281, 284–289: `.forge/guide/current.json`, `render.py answer <КОД> <ok|redo|discuss>`, `render.py build [--major] [--dry-run]`, `phase: guide`, `d['version']`, `d['built_at']`, схема с `codes.letters/map/screens/glossary.meaning`
  => Везде: файл — «последний `.forge/guide/vX.Y.json`» (в тестах — `.forge/guide/v1.0.json`, условие `ls .forge/guide/v*.json`); `answer` → `verdicts "<слова>"`; `build` → `init`/`bump` + `render` + `pdf`; `phase: project-guide`; поля — `d['meta']['version']`, `d['meta']['date']`; схема — из design-renderer Шаг 1. Тест statusline D2: `printf 'phase: project-guide\n'`.
- design-integration.md, D5 (стр. 129) и D6 (стр. 158, 160), INTERFACES стр. 293: «`current.json` + снимок `vX.Y.json`», статусы `open → accepted/redo/discuss`, «render.py уменьшает до 1200px или пропускает с пометкой», P2 «кто что утверждает» всегда, «07 план из findings block»
  => Версии — только `vX.Y.json`; статусы решений `default|accepted|changed|discuss|open|works|dropped`; лимиты снимков (≤6, PNG ≤200 КБ, 1280×800 css-scale) — правило СКИЛЛА, рендерер картинки не уменьшает (в плане такого кода нет) — фразу про 1200px убрать; P1 обязателен, P2 нет; план 07 — из `plan{now,next,later}`, который пишет скилл.
- design-integration.md, INTERFACES стр. 294 и D11 стр. 231: PDF-команда `--headless=new --disable-gpu --no-pdf-header-footer --print-to-pdf=…` напрямую через subprocess.run/командой
  => PDF только через `render.py pdf` (Popen + ожидание файла + terminate, флаги из плана Шаг 5; Chrome 152 сам не выходит). Макет: `render.py pdf .forge/sketches/project-guide-mockup.html`.
- design-integration.md, D13 (стр. 253): «затем новый `render.py merged project-guide`» руками
  => Не вызывать вручную — это делает блок «Гайд по проекту (Фаза 5)» finishing (Шаг 8в); в Чекпоинте C только проверить строку «гайд устарел на 1 задачу» при старте сессии.

## Пропуски
- ПОКРЫТИЕ КРИТЕРИЯ (сверка): (1) «собери гайд» → docs/guide/guide-v1.0.html + PDF, разделы 00–08 — Шаги 4, 5 (pdf), 7, Чекпоинт B п.1; (2) повторная сборка +0.1 / «версия 2» → 2.0 / все версии / guide-latest — Шаг 5 (bump), Шаг 7 (режим СОБРАТЬ), Чекпоинт B п.3; (3) ответы кодами — Шаг 5 (verdicts), Шаг 7, Шаг 8а (текст хука), Чекпоинт B п.2; (4) экраны веб/Telegram/ничего — Шаг 4 (render_screens/tgmock), Шаг 2 (фикстура), Шаг 7 (5а/5б брифа); живьём проверяется только «ничего» (Чекпоинт B), веб/tgmock — только фикстурой на Чекпоинте A; (5) язык на «вы», шапка — Шаги 2, 4, 7, evals Шаг 9; (6) merged/summary/«открой гайд» — Шаги 5, 8а/8в, 7, Чекпоинты B п.5 и C; (7) старый формат не собирается / документация / 7.8.0 — Шаги 1, 6, 9, 10, 11, Чекпоинт C. Все пункты закрыты при условии правок ниже.
- Шаг 1 → Шаг 3 (11): после `git mv` фикстуры `status-report-sample.json` → `guide-sample.json` (и переписывания в Шаге 2) тест миграции (11) остаётся без легаси-фикстуры (`LEGACY=$FIX/status-report-sample.json` из design-renderer Шаг 2). Решение: в Шаге 1 фикстуру копировать (`cp`), старую оставить; ожидание `grep -c '^R'` → 7; в Шаге 12 к списку допустимых остатков добавить `tests/hooks/fixtures/status-report-sample.json`.
- Шаг 7 (бриф SKILL.md): нет правила для находок с owner decision|both против решений группы O. Рендерер считает «ждут» = default|open|discuss решения + open-находки decision|both, поэтому если Секретарь заведёт O1 из f4, summary посчитает дважды (риск отмечен в design-renderer RISKS). Нужна одна фраза в брифе: «находка owner decision|both, по которой заведено решение O<n>, получает `links: ["O<n>"]` и `status: deferred` (deferred_reason «вынесено в O<n>»); в разделе 06 не показывается, в summary не считается». Тогда Чекпоинт B после сборки даст «ждут 5» (P1 + O1–O4), а не 9.
- Шаг 7/9: текст `tests/skill-triggering/prompts/project-guide.txt` нигде не переписывается (Шаг 1 — «только перенос», Шаг 9 файл не перечислен). Сейчас там «Собери мне отчёт документом — что в проекте чинить тебе, а что решать мне…» — это фраза старого формата. Взять строку из design-skill G4 п.1 («Собери мне гайд по проекту — такой документ, чтобы человек со стороны понял…») — добавить в Шаг 7 (после skill-creator) или Шаг 9.
- Шаг 7 (бриф): критерий «устаревшее убирается» — в брифе нет правила «решение, потерявшее смысл → `status: dropped` (номер занят, в HTML не печатается)» и «находка, которую агент доказал закрытой → done + done_at». Оба есть в design-renderer (Стабильность кодов) и design-skill G3 Шаг 3 п.2 — включить в бриф явно.
- Шаг 9: не перечислены стойкие формулировки без ключевых слов свипа — CLAUDE.md:131 («после мержа отчёт обновляется сам»), commands/init.md:592 («отчёт собирается только по слову, а после мержа обновляется сам»), docs/forge-runtime-flow.md:12 («отчёт «Что дальше» (ждут N…)» — в D5 есть), tests/hooks/test-session-start.sh:3 (комментарий «напоминание по отчёту «Что дальше»» — есть в D2 :3, но в плане Шаг 8а не упомянут). Добавить в Шаг 9/8 как точечные замены «отчёт» → «гайд».
- Шаг 11 map.yml:74 `files: 47` — при сохранении легаси-фикстуры (см. выше) файлов в tests станет 48; пересчитать `find forge-plugin/tests -type f | wc -l` после Шага 2, не переписывать вслепую.
- Шаг 8а: в тесте session-start новая проверка (6) пишет `.forge/status-report.json` и должна убедиться, что при этом `.forge/guide/` нет (`rm -rf .forge/guide`), иначе ветка `if ls .forge/guide/v*.json` перехватит. В design-integration D2 стоит `rm -f .forge/guide/current.json` — заменить на `rm -rf .forge/guide`.
- Стыки/Шаг 4: лимит снимков «≤6, PNG ≤200 КБ» никем не проверяется механически — рендерер только встраивает; в брифе Шага 7 закрепить как правило скилла (снимать `browser_resize 1280×800`, `scale: css`, не fullPage; больше 6 — не снимать). Иначе размер `docs/guide/*.html` в git не ограничен.

## Неверные фрагменты
- План Шаг 10, проверка `for c in … tgmock; do grep -c "class=\"$c" reference-vespera-guide.html` → «все ≥1»: в эталоне `class="tgmock` в разметке отсутствует (0), стили `.tgmock/.tgb/.tgbtn/.sysnote` есть только в CSS (строки 97–101). Заменить на `grep -c '\.tgmock' …` → ≥1 или убрать tgmock из цикла. Остальные классы: facts=1 week=1 howto=1 scheme=1 fstep=7 dcs=26 risk=18 rmap=1 gl=1 shots=5 — ок.
- План Шаг 9 и Шаг 12, свип `grep -rn 'status-report\|status_report\|Что дальше\|FORGE-REPORT\|reports/shots' … | grep -v '…status-report.json"\|старый отчёт'` → «пусто»: после реализации останутся легитимные попадания, не покрытые исключениями — `skills/project-guide/render.py` (`Path(".forge/status-report.html").unlink`, `".forge/reports/shots/"` в do_init — design-renderer Шаг 3 стр. 339–347), `tests/hooks/test-project-guide.sh` (проверка (11): `cp "$LEGACY" .forge/status-report.json; echo x > .forge/status-report.html`, текст check «migrate legacy status-report.json»), `tests/hooks/test-session-start.sh` (проверка (6): `> .forge/status-report.json &&`), фикстура `fixtures/status-report-sample.json` (по имени). Добавить в grep -v: `^./forge-plugin/tests/hooks/\|skills/project-guide/render.py`. Design-integration D9 стр. 223 список остатков тоже неполный.
- design-skill.md G6 (стр. 498): «finishing :140 `в отчёте отмечена сделанной`» — реальная строка 139 (`*«Карточка «<название>» в отчёте отмечена сделанной»*`); :137 — строка с `FORGE-REPORT: карточки с задачей «…» нет`. design-integration D3 «:131-138» — блок реально 131–139.
- design-integration.md D4 (стр. 86): «новая проверка (13) в конце» — физически последняя в файле проверка (8) (строки 184–192, порядок 1–7, 9–12, 8); добавлять после (8), нумерация комментария «(13)» условная. Проверка (3) — строки 87–88 верно.
- design-integration.md D8 стр. 185: index.yml «:51-58 session» — блок `session:` начинается со строки 51, поля `started` :52 … `next` :57, `last_session` :60 — верно; catalog `status-report:` — строки 47–49 (проверено). Замечание: catalog `path: .forge/guide/current.json` → `path: .forge/guide/` (стык).
- design-renderer.md Шаг 2 стр. 258: «RED: python3: can't open file» — неверно: после Шага 1 плана render.py уже лежит по новому пути, RED будет по содержанию (старый формат вывода), не по пути. Ожидание плана Шаг 3 (`N test(s) FAILED`) корректно.
- Все прочие якоря проверены и на месте: session-start.sh :52–59 (комментарий :52–53, `report_warn` :54–59), :73 строка Phase 5; statusline.sh :33; finishing :131, :133, :135, :137, :139, `git add -A` :94; new-task :106, :108, :110; backup.sh :2, :35–44 (heredoc, `status-report.html` :42, `reports/shots/` :43), :47, :52, :54, :57; init.md :584–585, :628, :664, :799–800, :861; test-memory-backup.sh :87–88; run-all.sh :20; forge-tray-mac.py :12, :27, :61; COMMANDS.md :14, :16, :29, :42, :291, :304, :424, :435, :850, :852, :960, :1059–1060; CLAUDE.md :83, :90, :119–122, :180, :209; memory-backup/SKILL.md :46; unblocker :217, :246, :256; using-forge :121; unblocker.yml :16; README :19, :55; GUIDE.md :153, :432; runtime-flow :128, :130, :173, :192, :204, :209, :218, :312–313; reference `<style>` :8–:110 (CSS = строки 9–109); render.py DEFAULT_* :26–27, FORGE_IGNORE :41–42, ReportBroken :100, img_tag :115, ensure_gitignore :260/:268, cd_repo_root :294; SKILL.md status-report заголовки :83, :112, :125, :138, :156, `## Шаг 3` :169; decisions.yml — 11 записей, `status-report-phase-5` :4; index.yml 2213 байт; evals/criteria/status-report.yml — 7 проверок; Chrome по пути есть; `skills/status-report/__pycache__` есть (rm в Шаге 1 нужен); ветка feat/project-guide активна; docs/status-report-format.md, sketches/status-report-mockup.html, prompts/status-report.txt — трекнуты (git mv/rm сработают).

## Лишнее
- design-skill G1: алиас `commands/status-report.md` и абзац про `$ARGUMENTS` в guide.md — решение плана «алиаса нет», команда из 3 строк.
- design-skill G4: `prompts/product-mapping.txt` и вторая строка в run-all.sh — негативная граница проверяется `project-unblocker.txt` и NOT-for в description; отдельный prompt-файл харнесс не требует.
- design-renderer Шаг 6: перевод `done`-находок прошлой версии в `deferred` при bump (3 строки) — в плане не принято, «сделано» и так зачёркнуто в конце яруса, а история — в changelog.
- design-skill G2 Секретарь: обязательный P2 «Кто утверждает что» — плагин гарантирует только P1; P2 пусть появляется, если в памяти есть зоны ответственности.
- design-integration INTERFACES: своя схема JSON (`codes.letters`, `map`, `screens`, `glossary.meaning`) и `build --dry-run` — целиком заменены схемой рендерера; исполнителю не читать.
- design-integration D13: ручной `render.py merged project-guide` после мержа — дублирует блок finishing из Шага 8в; оставить только проверку строки напоминания.
- design-integration D6/D5: фразы «render.py уменьшает снимки до 1200px» и «в футере «обновлено после мержа: N задач»» в форматном доке — рендерер этого не делает; из Шага 10 убрать, чтобы документ не обещал несуществующее.
- План Шаг 12: живой прогон `run-test.sh project-unblocker …` (минуты на claude CLI) — критерий не требует; достаточно позитивного `project-guide` + NOT-for в description (trigger eval skill-creator в Шаге 7 уже покрывает негатив «что дальше по проекту»).
