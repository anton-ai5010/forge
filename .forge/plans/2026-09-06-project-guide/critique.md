# Критика плана project-guide (2026-09-06): 43 устоявшие находки, 3 опровергнуты

## Блокеры (0)

## Важное (36)

### (skeptic) Ответ кодом без тире/двоеточия («A2 ок», «А2 принято» — так и даёт голосовой ввод) парсер не разбирает вовсе: VERDICT_RE требует `[—–\-:]+` после кода. При нуле
- Где: План, Стыки (строка 27) и Шаг 5 (`parse_verdicts`); design-renderer.md:561-562 (CODE/VERDICT_RE), :637 (печать said); design-skill.md:97, :378
- Проблема: Ответ кодом без тире/двоеточия («A2 ок», «А2 принято» — так и даёт голосовой ввод) парсер не разбирает вовсе: VERDICT_RE требует `[—–\-:]+` после кода. При нуле совпадений run() печатает `FORGE-GUIDE: ` + пустой список — выглядит как успех, статусы не тронуты, предупреждения нет. Скилл при этом сам обещает форму «A2 ок» (design-skill.md:378), а его роутинг (стр. 97) тоже требует тире — сообщение вообще не попадёт в режим ОТВЕТЫ КОДАМИ.
- Правка: Шаг 5 (`parse_verdicts`, design-renderer.md:562): разделитель после кода — необязательный, но код без ничего после него (конец строки) вердиктом не считать: `VERDICT_RE = re.compile(rf"(?<![\w])({CODE})(?:\s*[—–\-:]+\s*|\s+)(.*?)(?=[\s.;,]*(?<![\w]){CODE}(?:\s*[—–\-:]|\s)|\s*$)", re.S)`. Проверено на Python: `'A2 ок'`, `'А2 принято'`, `'A2 ок. B2 переделать: текст'` разбираются, эталонная строка из «Как проверим» Шага 5 даёт прежний результат. Побочный эффект: код внутри текста ответа («… и код A1 тоже») попадёт как отдельный вердикт с пустой заметкой → уйдёт в `⚠ не понял вердикт`, статус не меняется — приемлемо; в `apply_verdicts` ветку `verb is None` дополнить `if note: d["note"] = note`, чтобы пустая заметка не затирала существующую. В `run()` (design-renderer.md:637): строку `FORGE-GUIDE: …said` печатать только при непустом `said`; если `said` и `warn` пусты — `FORGE-GUIDE: ⚠ кодов в ответе не нашёл — жду вид «A2 — ок. B2 — переделать: …»`. Шаг 7 (бриф skill-creator): роутинг ОТВЕТЫ КОДАМИ — «сообщение содержит код `[A-ZА-Я]{1,2}\d+` и после него слово-ответ (ок/переделать/обсудить/…); тире или двоеточие необязательны» (заменяет design-skill.md:97); в таблицу :378 форма «A2 ок» остаётся как есть — теперь она правдива. Шаг 3/тест (8) design-renderer.md:160-174: добавить `out=$(render verdicts "A2 ок. B2 переделать: только по телефону")` → A2 `accepted`, B2 `changed` + `what` = «только по телефону»; и `render verdicts "просто текст без кодов"` → в выводе «кодов в ответе не нашёл», JSON не изменился (`updated_at` прежний или файл побайтово тот же).

### (skeptic) Ответ на открытый вопрос группы O («O1 — ночью отвечает администратор») и выбор варианта для «обсудить» («D1 — пополам») — это свободный текст без глагола. `app
- Где: План, Стыки (строка 27: список глаголов только ок/переделать/обсудить/сделано); Шаг 5 `apply_verdicts`; design-renderer.md:606-609
- Проблема: Ответ на открытый вопрос группы O («O1 — ночью отвечает администратор») и выбор варианта для «обсудить» («D1 — пополам») — это свободный текст без глагола. `apply_verdicts` для решения с verb=None пишет note, статус не меняет, 🔥 остаётся, в чат «не понял вердикт». То есть основной сценарий O-группы (владелец отвечает на вопрос, у которого «наших предложений нет») сломан: вопрос никогда не закрывается ответом, только словом «ок». Скилл же обещает форму «O1 — <ответ>» (design-skill.md:81, :453).
- Правка: Шаг 5 плана + «Стыки» строка 27 — добавить правило: для решения со status `open` или `discuss` текст без распознанного глагола = ответ. Минимальный код в `apply_verdicts` (design-renderer.md, перед `if verb in ("accepted", …)`):

```python
if verb is None and d.get("status", "open") in ("open", "discuss") and note:
    verb = "accepted"                     # свободный текст на открытый вопрос = ответ
    d["was"], d["what"] = d.get("what", ""), note
```
Дальше срабатывает существующая ветка accepted: status=accepted, fire=False, verdict={date, text: note}, said «O1 → принято: ночью отвечает администратор», pending_changes «{дата}: O1 — принято (ночью …)». Для `default`/`accepted`/`changed` поведение прежнее («не понял») — тест «A1 — ну не знаю» (design-renderer.md:173, A1 к тому моменту accepted) не ломается.

Тест (8) в test-project-guide.sh — дописать после существующих проверок: `out=$(render verdicts "D1 — пополам")` → status D1 = accepted, what = «пополам», fire = False, в `$out` нет «не понял», `grep -o '<span class="fire">' "$LATEST" | wc -l` = 0 (после первого вызова 🔥 остаётся только у D1). Опционально: вернуть O1 в `open` через python-правку JSON (как в :132-134) и проверить «O1 — ночью отвечает администратор» → accepted, what обновлён.

Синхронно: «Стыки» :27 — фраза «для open/discuss текст без глагола → accepted, what=текст, was=старый»; докстринг режима `verdicts` (design-renderer.md:747) и пример в Шаге 5 «Как проверим» — добавить `('O1', …)` со свободным текстом; design-skill.md таблица Шага 8, строка «O1 — <ответ>» — заменить `verdict O1 answer` на «тот же `verdicts` — свободный текст на O/обсуждаемое решение парсер сам считает ответом».

Опционально (та же ветка): в `parse_verdicts` брать note как хвост после совпавшего ключа (`rest[len(k):].lstrip(" ,:—–-")`), а не только после «:», чтобы «O1 — да, ночью отвечает администратор» не терял текст ответа.

### (skeptic) Разбор глагола хрупок для живой речи: (а) текст правки берётся только после `:` (`rest.split(":",1)`), поэтому «B2 — переделать — только по телефону» или «перед
- Где: План Шаг 5 (`VERBS` + `parse_verdicts`); design-renderer.md:563-571 (VERBS), :582-584 (re.match + split по «:»)
- Проблема: Разбор глагола хрупок для живой речи: (а) текст правки берётся только после `:` (`rest.split(":",1)`), поэтому «B2 — переделать — только по телефону» или «переделать, только по телефону» дают status=changed с нетронутым `what` и без `was` — в гайде чип «переделано», а текст старый; (б) глагол ищется `re.match` с начала строки, значит «давайте обсудим», «лучше переделать: …», «нет, обсудить» → «не понял»; (в) слов отказа («нет», «не согласен», «против», «не надо») в VERBS нет вообще — отказ от «дефолта» никак не выражается.
- Правка: В Шаге 5 плана (и в «Стыках» строка 27 одной фразой) уточнить parse_verdicts/apply_verdicts из design-renderer.md:563-602:
1) Ключ искать по всей фразе, а не с начала: `m = re.search(rf"(?<![а-яa-z]){re.escape(k)}(?![а-яa-z])", low)`; порядок групп VERBS сохранить как приоритет (changed → discuss → отказ → up → down → deferred → works → accepted), чтобы «ок, но обсудить» и «нет, обсудить» давали discuss, а «да, так и оставляем» — accepted.
2) note = `rest[m.end():].lstrip(" :,—–-.")` (текст после ключа при любом разделителе); если пусто и ключ не в начале — note = вся фраза.
3) Добавить группу отказа `(("нет", "не согласен", "не согласна", "против", "не надо", "не пойдёт"), "discuss")` — для решений статус discuss (🔥 остаётся), note = вся фраза, чтобы Клод по таблице скилла предложил 2–3 варианта. Для R-кодов ветки discuss нет — оставить «не понял» с заметкой (или отдельно решить: отказ по риску = deferred).
4) `changed` без текста (и для decisions, и для findings): статус НЕ менять, `continue` без записи в said/pending_changes, предупредить `⚠ B2: «переделать» без текста — напиши «B2 — переделать: <как надо>»`. Сейчас в :600 статус ставится до проверки note.
5) Если parse_verdicts вернул пусто, а в тексте есть токен вида CODE (`[A-ZА-Я]{1,2}\d{1,3}`) — печатать `⚠ не нашёл ответов вида «A2 — ок»` (закрывает молчаливую потерю «A2 ок»); либо расширить разделитель в VERDICT_RE до `(?:\s*[—–\-:]+\s*|\s+)`.
6) В тест (8) test-project-guide.sh добавить проверки: «B1 — переделать — по телефону» → status changed, what=«по телефону», was=старое; «B1 — переделать» → статус прежний + ⚠ «без текста»; «A1 — давайте обсудим» → discuss; «A1 — нет» → discuss, fire=true; «A1 — лучше переделать: X» → changed, what=X; «A1 ок» → ⚠ (или accepted при варианте с расширенным разделителем). Обновить ожидаемый вывод в «Как проверим» Шага 5 (примеры остаются валидны).

### (skeptic) Таблица нормализации кириллицы переводит русскую «Р» (эр) в латинскую «P» по внешнему сходству. Голосовой ввод «Р4 — приоритет выше» (риск R4) превращается в P4
- Где: План Шаг 5 / design-renderer.md:560 `CYR = str.maketrans("АВСДОРПЕНКМТ", "ABCDOPPEHKMT")`
- Проблема: Таблица нормализации кириллицы переводит русскую «Р» (эр) в латинскую «P» по внешнему сходству. Голосовой ввод «Р4 — приоритет выше» (риск R4) превращается в P4: либо «код P4 не найден», либо — если P4 существует — тронуто чужое решение группы «Процесс».
- Правка: Шаг 6 плана (design-renderer.md:560–635), три правки: (а) в таблице CYR кириллическую Р переводить в R (фонетика голосового ввода — заявленная причина таблицы), П → P оставить: `CYR = str.maketrans("АВСДОРПЕНКМТ", "ABCDORPEHKMT")`; (б) в `apply_verdicts` добавить «близнеца» P↔R: если код не найден ни в by_code, ни в by_risk и начинается с P или R — попробовать `("R" if code[0]=="P" else "P") + code[1:]`; дополнительно, если код P<n> есть, но глагол рисковый (`up`/`down`/`deferred`) и R<n> существует — применять к риску вместо предупреждения на :611 (это ловит визуальное прочтение Р→P, когда оба кода есть); (в) при таком перекладывании печатать в said/warn «Р4 прочитал как R4 (риск); если имели в виду P4 — напишите латиницей». Тест (8) (design-renderer.md:160): заменить `R4 — приоритет выше` на `Р4 — приоритет выше` с кириллической Р (проверка f4 → crit уже есть, счётчик pending_changes = 5 не меняется) и добавить проверку, что `d['decisions']` P1 остался `default`; в ручной проверке Шага 6 (:639) добавить кириллическую `Р4` в строку parse_verdicts с ожидаемым `('R4','up','')`. Зафиксировать в «Стыках» (план :27) правило: «кириллическая Р = R (риск), П = P».

### (skeptic) Ожидание теста (9) «ждут 4 решения владельца» не сходится с правилом подсчёта самого плана. После verdicts из (8): ждут решения P1 (default) + D1 (discuss) = 2;
- Где: План Шаг 3 (группа проверок (9)) и Шаг 5; design-renderer.md:183 (`grep -q 'ждут 4 решения владельца'`), :71 (ожидаемые числа фикстуры), :503-504 (формула open_decisions)
- Проблема: Ожидание теста (9) «ждут 4 решения владельца» не сходится с правилом подсчёта самого плана. После verdicts из (8): ждут решения P1 (default) + D1 (discuss) = 2; находки open с owner decision|both — f2 (both), f3, f4 (decision) = 3 (их статус verdicts/merged не трогают; R4 «приоритет выше» меняет только block). Итого 5 — тест будет красным на зелёном коде, а Шаг 5 «Как проверим» требует All tests passed.
- Правка: В плане Шаг 3 (группа (9)) и в design-renderer.md:183 заменить `ждут 4 решения владельца` на `ждут 5 решений владельца` (именно «решений» — plural(5) даёт эту форму, grep на «4 решения» с «5 решений» не совпадёт). Перед RED пересчитать и зафиксировать комментарием в тесте вывод числа: decisions P1 (default) + D1 (discuss) = 2, находки open с owner decision|both f2/f3/f4 = 3 → 5; проверки (1) «ждут ответа 5, 🔥 3» и финальная «тишина» (все accepted + decision-находки done) остаются как есть. Формулу open_decisions (:503-504) не трогать.

### (skeptic) `link` принимает только внутренний id находки (`f7`), но new-task 9.5 по плану говорит «`<id>` — id находки (f7) или код (R7)», а владелец в гайде видит только 
- Где: План Шаг 5 («merged/link/summary — как в v1») и Шаг 8г; design-renderer.md:700 («link: как в v1, строки 317-358»); текущий render.py:349 `hit = [f for f in … if f.get("id") == fid]`
- Проблема: `link` принимает только внутренний id находки (`f7`), но new-task 9.5 по плану говорит «`<id>` — id находки (f7) или код (R7)», а владелец в гайде видит только R-коды, и скилл передаёт `card:<id>` тем, что напечатано. `link R7 <slug>` ответит «карточки R7 нет» (exit 0, тихо), task_slug не привяжется — и finishing после мержа не отметит пункт сделанным.
- Правка: 1) План Шаг 5 (и design-renderer.md Шаг 8 п.1): режим `link` — разрешать `fid` как внутренний id ИЛИ R-код, той же нормализацией, что в `parse_verdicts`: 
```python
code = fid.upper().translate(CYR)
hit = [f for f in data.get("findings", []) if f.get("id") == fid or risk_code(f) == code]
```
Сообщения оставить как в v1 («карточки {fid} нет» / «карточка {fid} → задача {slug}»). Коды решений (`P2`, `A1`, `O1`) в link НЕ принимать — это не находки; в тексте new-task 9.5 брать формулировку плана (строка 133: «`f7` или `R7`»), а не design-integration D3 («R3 или P2»).
2) План Шаг 3, группа (9): добавить проверку `render link R2 some-task` → у f2 `task_slug == "some-task"` и в stdout «карточка R2 → задача some-task»; опционально `link р2 x` (кириллическая р) → тот же f2.
3) План Шаг 7 (бриф skill-creator): явно «хэндофф «бери» → new-task с `card:R<n>` (код, который видит владелец); `link` принимает и `R<n>`, и `f<n>`» — чтобы скилл и рендерер говорили на одном языке.

### (skeptic) После ответов кодами и после мержа рендерер перерисовывает `docs/guide/guide-vX.Y.html` + `guide-latest.html`, а `guide-vX.Y.pdf` той же версии не трогает. PDF 
- Где: План, Стыки (строка 25: `pdf` отдельный режим; строка 22: verdicts/merged правят последний vX.Y.json на месте), Шаг 5 (`verdicts`/`merged` → `save` + `do_render`), Шаг 7 (режим ОТВЕТЫ КОДАМИ), Шаг 8в (finishing)
- Проблема: После ответов кодами и после мержа рендерер перерисовывает `docs/guide/guide-vX.Y.html` + `guide-latest.html`, а `guide-vX.Y.pdf` той же версии не трогает. PDF — именно то, что уходит человеку со стороны (эталон Vespera пришёл PDF-ом), и он будет показывать 🔥 у уже принятого A2 и «в работе» у влитой задачи до следующей полной сборки. В плане PDF только через отдельный `render.py pdf`, а режим ОТВЕТЫ КОДАМИ и блок finishing его не зовут.
- Правка: Править на стороне рендерера (одно место — покрывает хук, скилл и finishing разом), а не в трёх брифах:

1. Шаг 5 (design-renderer.md Шаг 6 и Шаг 8 п.1): в `run()` для режимов `verdicts` и `merged` после `save` + `do_render(jp)` добавить: `pdf = DOCS_DIR / f"guide-v{ver_of(data)}.pdf"; if pdf.is_file(): do_pdf(DOCS_DIR / f"guide-v{ver}.html", pdf)`. Условие «только если PDF этой версии уже есть» держит тесты/макет быстрыми (Chrome не запускается, пока PDF не собирали) и не трогает поведение без Chrome (внутри `do_pdf` уже тихий пропуск, exit 0). `link` не трогать — он не меняет видимого содержимого.
2. Шаг 3 / design-renderer.md тест (15): после ветки «с Chrome — файл %PDF» добавить в ту же ветку: `m1=$(stat -f %m docs/guide/guide-v1.0.pdf); render verdicts "A1 — ок"; [ "$(stat -f %m …pdf)" != "$m1" ]` → `check "verdicts should rebuild the existing PDF of the live version"`; без Chrome — SKIP как у (15). Проверка (14) битого JSON не меняется.
3. Стыки :22 дописать: «…перерисовывают `guide-vX.Y.html` + `guide-latest.html`, и если `guide-vX.Y.pdf` уже есть — перестраивают его тем же `do_pdf`». :38 переформулировать: «PDF собирает только `do_pdf` (режим `pdf`; `verdicts`/`merged` зовут его для уже существующего PDF последней версии)».
4. Чекпоинт B п.2: вместо «`git diff --stat docs/guide/guide-v1.0.html`» ждать три файла — `git diff --stat docs/guide/` → `guide-v1.0.html`, `guide-v1.0.pdf`, `guide-latest.html`.
5. Тот же пробел у режима «добавь снимки» (Шаг 7 бриф: «Edit JSON + `render`, без агентов»): дописать в бриф «+ `render.py pdf`» — `render` намеренно PDF не собирает (иначе при полной сборке Chrome запускался бы дважды).
Плата: в finishing после мержа +3–10 с на Chrome (потолок 60 с при зависании) — приемлемо; в тестах Chrome стартует только в явной PDF-ветке.

### (skeptic) backup.sh: условие `[ -d docs/guide ]` добавляет `docs/guide` в pathspec коммита, но если в папке нечего закоммитить — она пуста (render создал dir и упал / фай
- Где: План, Стыки (строка 34) и Шаг 8д; design-integration.md:106-112
- Проблема: backup.sh: условие `[ -d docs/guide ]` добавляет `docs/guide` в pathspec коммита, но если в папке нечего закоммитить — она пуста (render создал dir и упал / файлы удалены руками) или `docs/` целиком под .gitignore в чужом проекте (`git add` молча упирается в игнор) — `git commit -- .forge docs/guide` падает с «pathspec 'docs/guide' did not match any file(s) known to git», и память НЕ сохраняется (сообщение «коммит памяти не прошёл»). План сам замечает, что несуществующий pathspec роняет commit, но проверяет только существование каталога.
- Правка: Правка к Шагу 8д (и к design-integration.md:106-110, стр. 34 плана): включать `docs/guide` в pathspec только когда под ней есть ЗАСТЕЙДЖЕННЫЕ изменения, а не по существованию каталога и не по `ls-files --cached` (последний пропустит удаления трекнутых файлов — они останутся в индексе после коммита `.forge` и уедут в чужой следующий коммит):
```bash
git add .forge >/dev/null 2>&1 || true
paths=(.forge)
if [ -d docs/guide ]; then
    git add docs/guide >/dev/null 2>&1 || true   # под игнором / пусто — тихо ничего
    git diff --cached --quiet -- docs/guide 2>/dev/null || paths+=(docs/guide)
fi
```
Проверено на 6 сценариях: пустая папка, `docs/` под игнором, `*.html` под игнором → commit только `.forge`, rc=0; новый файл → уезжает вместе; трекнутые без изменений → `.forge` only; трекнутые удалены руками → удаление уезжает тем же коммитом. В test-memory-backup.sh добавить проверку (14): `mkdir -p docs/guide` (пустая) + грязный `.forge` → `bash backup.sh` печатает «сохранена», `git log -1 --stat` содержит `.forge`, `.forge/.last-backup` существует, нет строки «коммит памяти не прошёл»; и подвариант с `printf 'docs/\n' > .gitignore` + `echo x > docs/guide/guide-v1.0.html` → то же, `git status --short` не содержит `docs/guide` в стейдже. В Шаге 8д «Как проверим» — ожидать 14 PASS вместо 13.

### (skeptic) `init` при битом легаси `.forge/status-report.json` бросает GuideBroken с текстом «скажи «собери гайд», пересоберу» — а «собери гайд» снова зовёт `init`, которы
- Где: План Шаг 4 (`do_init`) / design-renderer.md:333 `old = load(LEGACY_JSON)  # битый → ReportBroken, гайд не создаём`, :356 (текст сообщения)
- Проблема: `init` при битом легаси `.forge/status-report.json` бросает GuideBroken с текстом «скажи «собери гайд», пересоберу» — а «собери гайд» снова зовёт `init`, который снова падает на том же файле. Тупик без выхода для проекта на 7.7.0 с испорченным отчётом: гайд создать нельзя, подсказка ведёт по кругу; session-start при этом каждую сессию предлагает миграцию.
- Правка: План Шаг 4 (do_init по design-renderer.md Шаг 3, :331-349) — переписать блок миграции так:

had_legacy = LEGACY_JSON.is_file()
if had_legacy:
    try:
        old = load(LEGACY_JSON)
    except GuideBroken as e:
        quarantine = LEGACY_JSON.with_name(LEGACY_JSON.name + ".broken")   # .forge/status-report.json.broken
        LEGACY_JSON.rename(quarantine)
        print(f"FORGE-GUIDE: старый отчёт повреждён ({e.__cause__}) — отложил в {quarantine}, гайд создаю пустым")
        old = {}
    (перенос findings/sources/project/eyebrow/снимков — как в черновике; при old={} moved остаётся 0)

и условие уборки (:341) `if moved or LEGACY_JSON.is_file():` → `if had_legacy:` (иначе после rename LEGACY_JSON.is_file() уже False и status-report.html/снимки не уберутся). `LEGACY_JSON.unlink(missing_ok=True)` оставить — при карантине файла уже нет, missing_ok спасает. Итоговая строка при moved==0 и had_legacy — «создан … (пустой каркас, P1 заведён)» как в черновике.

Почему `.json.broken` рядом, а не в FORGE_IGNORE: список игнора зафиксирован стыками на 7 строк (план :24, тест (12), heredoc backup.sh и init.md) — не трогать. Файл `.broken` уедет в память через backup.sh (`git add .forge`) — это допустимо и даже полезно (улика), в сообщении сказать владельцу, что его можно удалить. Имя без `status-report.json` ровно → `elif [ -f .forge/status-report.json ]` в session-start.sh (Шаг 8) перестаёт срабатывать, `cd_repo_root` тоже не путается.

Тест: в test-project-guide.sh к (11) (план Шаг 3 :79, design-renderer Шаг 2) добавить проверку:
new_project; printf '{ "findings": [ обрыв' > .forge/status-report.json; echo x > .forge/status-report.html
out=$(render init); rc=$?
[ "$rc" -eq 0 ] && [ -f .forge/guide/v1.0.json ] && [ -f .forge/status-report.json.broken ] && [ ! -f .forge/status-report.json ] && [ ! -f .forge/status-report.html ] && printf '%s' "$out" | grep -q 'повреждён' && grep -q '"P1"' .forge/guide/v1.0.json
check "init should quarantine a broken legacy report and still create v1.0" $?

Свип Шага 9/12 (план :152): текст сообщения содержит «старый отчёт» — это уже в grep -v; но строка `LEGACY_JSON.name + ".broken"` литерала `status-report` не содержит, так что исключений дописывать не нужно (либо, как советует design-integrator.md:68, исключить `skills/project-guide/render.py` целиком).

Заодно поправить текст `load()` (:356): «файл гайда повреждён» вводит в заблуждение, когда path = старый отчёт; достаточно «FORGE-GUIDE: файл повреждён ({path}: {e}) — скажи «собери гайд», пересоберу» — после карантина эта подсказка для init уже честная.

### (skeptic) Сборка идёт «bump → 7 субагентов → Edit JSON → render»: `vX.Y.json` появляется первым, ещё пустым (копия прошлой с новым номером). Если сессия оборвётся или вла
- Где: План, Стыки (строка 22: «отдельных current/latest/draft.json нет», версия «живая» до следующей сборки) и Шаг 7 (последовательность СОБРАТЬ: `bump` → Edit → `render`)
- Проблема: Сборка идёт «bump → 7 субагентов → Edit JSON → render»: `vX.Y.json` появляется первым, ещё пустым (копия прошлой с новым номером). Если сессия оборвётся или владелец скажет «стоп» после bump, полупустая версия остаётся «последней»: session-start и `summary` считают по ней, `verdicts`/`merged` правят её, «открой гайд» показывает заглушки «раздел появится в следующей версии», а следующее «собери гайд» бампнет ещё раз — в истории дырка-пустышка v1.1, а v1.2 наследует недоделанный текст. Черновик (draft.json) план отверг, замены не дал.
- Правка: Минимальная замена атомарности флагом в JSON, без нового файла. Шаг 5 (render.py): `do_init` и `do_bump` ставят `meta.building = true`; режим CLI `render` без аргументов (боевой путь) снимает флаг перед `save`+рендером — только этот режим, `do_render` изнутри `verdicts`/`merged` флаг не трогает; `verdicts`/`merged` при `building` — сохраняют JSON, HTML не перерисовывают, печатают одну строку «FORGE-GUIDE: гайд vX.Y ещё не собран — HTML не перерисован, ответ учтён»; `bump` при `building` у последней версии — отказ «FORGE-GUIDE: vX.Y ещё не собрана — дособери её (Edit → render), новый номер не нужен», файл не создаёт; `summary` при `building` вместо счётчиков печатает «📖 Гайд по проекту vX.Y: сборка не закончена — скажи «собери гайд»». Шаг 3 (тест, группа (7)): после `bump` → `summary` содержит «не закончена»; повторный `bump` → отказ и `ls .forge/guide/v1.2.json` нет; после `render` → `building` отсутствует, повторный `bump` работает; `verdicts` при `building` не создаёт `docs/guide/guide-v1.1.html`. Шаг 7 (бриф skill-creator): в режиме СОБРАТЬ «`bump` (или `init`) запускать после 7 субагентов и синтеза, прямо перед Edit JSON; если `bump` отказал из-за незаконченной версии — не бампать, дособирать её же (Edit → render → pdf)»; строка «стоп» в таблице: «если сборка оборвана после `bump`, следующий «собери гайд» дособирает ту же версию». Шаг 8а (session-start): ничего менять не надо — хук печатает вывод `summary` как есть. Стыки стр. 22: дописать «после `bump`/`init` версия помечена `meta.building` до первого боевого `render`».

### (skeptic) Буквы групп решений «под проект» подбирает Секретарь без ограничений: план не запрещает букву R (занята рисками) и кириллические буквы. Если Секретарь заведёт г
- Где: План, Стыки (строка 28: «буквы под проект») и Шаг 7 (бриф Секретаря); design-renderer.md:597-613 (порядок by_code/by_risk), :560 (CYR)
- Проблема: Буквы групп решений «под проект» подбирает Секретарь без ограничений: план не запрещает букву R (занята рисками) и кириллические буквы. Если Секретарь заведёт группу «R» или код «Д1»: (а) `apply_verdicts` проверяет `by_code` раньше `by_risk` — ответ «R4 — приоритет выше» уйдёт в решение R4, риск не тронется; (б) кириллический код в JSON («Д1») после нормализации ответа («Д1»→«D1») не найдётся — «код D1 не найден», хотя в гайде он напечатан.
- Правка: Три точечные правки, все в рамках существующих шагов: (а) Шаг 7, бриф Секретаря (design-skill.md:224) — добавить строку: «Буква группы — одна ЛАТИНСКАЯ заглавная A–Z; P и O — служебные (процесс/открытые), R занята рисками раздела 06 — под проект не брать; код = буква + число (`A1`), кириллица в кодах запрещена». Дополнительно в Шаг 0 скилла (design-skill.md:97) детект ответов уже `[A-Z]\d+` — так бриф и детект совпадут. (б) Шаг 4, `groups()`/`do_render` — при рендере проверять каждый `decisions[].code` по `^[A-Z]\d+$` и букву ≠ R; несовпадение → `FORGE-GUIDE: ⚠ код Д1: буква не латинская` / `⚠ код R2: буква R занята рисками — ответ по нему уйдёт в риск`, рендер не прерывать (exit 0, как все режимы). (в) Шаг 5, `apply_verdicts` — коды `R\d+` искать только в `by_risk` (первой строкой цикла: `if code.startswith("R"): … by_risk …`), остальные — в `by_code`; тогда даже при коллизии ответ по R-коду не попадёт в решение. Шаг 3, тест (8) или новая проверка (16): временный JSON с решениями `R1` и `Д1` → `render` печатает оба предупреждения, `verdicts "R1 — ок"` закрывает находку f1, а не решение. Правило I/L из исходной правки не нужно — лишнее ограничение без реальной ошибки в парсере.

### (skeptic) Правило стыка: находка owner decision|both, вынесенная Секретарём в решение O<n>, получает `status: deferred` и «в разделе 06 не показывается». Но под картой ри
- Где: План, Стыки (строка 32) и Шаг 4 (`render_html` раздел 06: «после ярусов, если есть deferred: `<p class="gt">Отложено — N (…)</p>`»); design-renderer.md:489, :502
- Проблема: Правило стыка: находка owner decision|both, вынесенная Секретарём в решение O<n>, получает `status: deferred` и «в разделе 06 не показывается». Но под картой рисков рендерер печатает строку «Отложено — N (заголовки…)» по всем deferred — и туда попадут вынесенные находки. На этом репо после первой сборки: f4–f7 → O1–O4 в 05 и одновременно «Отложено — 6 (отделить факты от догадок; …)» в 06 — читатель со стороны видит один вопрос как открытый и как отложенный.
- Правка: Шаг 4 (render.py, по design-renderer.md :489/:502/:744): ввести один хелпер `moved_out(f)` = `f.get("status") == "deferred" and f.get("owner") in ("decision", "both") and any(re.fullmatch(r"O\d+", str(l)) for l in (f.get("links") or []))` (именно с проверкой owner: находка `owner: code`, отложенная по лимиту (design-skill.md:266), может ссылаться на O-код как перекрёстную ссылку и не должна пропадать из «Отложено»). Использовать его в трёх местах: `counts()["deferred"] = [f for f in fs if f.get("status") == "deferred" and not moved_out(f)]`; строка «Отложено — N (…)» под ярусами 06 строится из этого же отфильтрованного списка (нет ничего — строка не печатается); stdout `render` «отложено X» — тот же счётчик. `active` (карточки рисков) не трогать — moved_out и так исключены как deferred. Шаг 3 (test-project-guide.sh), группа (3): не менять f3 в фикстуре (сломает «находок 8 / deferred 1 / рисков 7 / ждут 8»); вместо этого добавить в guide-sample.json 9-ю находку `f9` (`owner: decision`, `status: deferred`, `links: ["O1"]`, `deferred_reason: "вынесено в O1"`, заголовок «Кто дежурит ночью») — при фильтре ни один ожидаемый счётчик не меняется (active 7, deferred 1, ждут — только open) — и проверять: `grep -q 'Отложено — 1' "$HTML" && ! grep -q 'Кто дежурит ночью' "$HTML" && ! grep -q '<span class="rc">R9</span>' "$HTML"`, а в stdout render — «отложено 1». Альтернатива без правки фикстуры — модифицированная копия через python-heredoc (как в test-status-report.sh:77/138/191). В формат-документ Шага 10 добавить фразу: «находка, вынесенная в O<n>, в карте рисков и в счётчике «отложено» не показывается — она живёт как открытый вопрос в 05».

### (pragmatist) 7 субагентов одним сообщением — Секретарь решений дублирует Аналитика 4. Оба читают одни файлы: decisions.yml, direction.yml, `.forge/tasks/*.md` без мержа, «##
- Где: План: Стыки «Субагенты сборки — 7 одним сообщением»; Шаг 7 бриф («7 субагентов одним сообщением», проверка `grep -c '^### Картограф\|^### Аналитик\|^### Секретарь' → 7`); design-skill.md G2 строки 217–243 (Секретарь) против текущего SKILL.md status-report строки 156–166 (Агент 4)
- Проблема: 7 субагентов одним сообщением — Секретарь решений дублирует Аналитика 4. Оба читают одни файлы: decisions.yml, direction.yml, `.forge/tasks/*.md` без мержа, «## Открытые вопросы» в планах, README-обещания. А синтез (Шаг 3 п.7 брифа) всё равно заново сверяет вывод Секретаря с decisions.yml («без source → максимум default», «каждая запись decisions.yml должна быть либо решением, либо осознанно пропущена») — то есть главная сессия делает его работу второй раз. decisions.yml в этом репо — 11 записей, direction.yml — один файл: это чтение на минуту в главной сессии, а не отдельный агент.
- Правка: Свести к 6 субагентам: Секретаря решений влить в Аналитика 4 («Память, документация и решения»). Конкретно:

1. design-skill.md G2: убрать раздел «### Секретарь решений» (строки 217–243); в промпт Аналитика 4 (сохранённый блок «### Аналитик 4») добавить: (а) строку преамбула Секретаря «Прошлая версия гайда: groups[], decisions[] code/title/status; или «нет»» (сейчас у аналитиков в преамбуле только список open-находок — design-skill:51 п.(в)); (б) правила кодов и статусов из строк 222–229 (коды не меняются/не переиспользуются; P и O всегда, 2–4 буквы под проект; accepted/works/default/open; P1 обязателен, P2 — если есть зоны ответственности — по «Стыки» 35; «каким способом» — не решение); (в) вторую секцию вывода «## Группы» + «## Решения (не больше 20)» в формате Секретаря (code/title/what/why/status/fire/source); (г) одно правило дедупа на входе: «один и тот же вопрос владельцу — либо решение группы O в «## Решения», либо находка owner: decision — никогда оба».

2. План: «Стыки» строка 29 → «6 одним сообщением: Картограф А, Картограф Б, Аналитики 1–4 (код / git / инфраструктура + «Адреса интерфейса» / память + документация + решения → decisions[] с кодами и группами)»; Шаг 7 бриф → «6 субагентов одним сообщением», проверка `grep -c '^### Картограф\|^### Аналитик' → 6` (Секретарь из grep убрать); «Поправки» → `sources.analysts` = 6; Чекпоинт B → «6 субагентов»; design-skill:251 проверка заголовков → 7 (без Секретаря); design-skill:274 и :286 → analysts 6; синтез п.7 (design-skill:267) переформулировать «Решения от Аналитика 4: …» — сверку с decisions.yml оставить как есть (дешёвая валидация статусов).

3. Шаг 2 план (фикстура guide-sample.json): `sources.analysts` → 6; в Шаге 3 проверить, не ждёт ли тест (1)/футер число агентов — если да, ожидание → 6.

4. «Стыки» строка 32 (links + deferred для находок decision|both) — оставить: Аналитики 1–3 тоже могут дать owner: decision.

Запасной вариант, если исполнитель считает, что композиция раздела 05 требует отдельного «писателя»: оставить 7, но вычеркнуть из Аналитика 4 пункты 3–5 (decisions.yml, tasks/«Открытые вопросы», direction.yml) — эти источники целиком уходят Секретарю, Аналитику 4 остаются README/CLAUDE.md и status.yml против кода. Тогда дублирование чтения и двойной список O-вопросов исчезают без слияния.

### (pragmatist) Парсер вердиктов на Python дублирует то, что Клод и так делает, читая сообщение владельца: `VERDICT_RE` с lookahead-ами, таблица `CYR` для кириллицы, 7 групп си
- Где: План: Стыки «Ответы кодами» (разделители, кириллица, R-глаголы, неизвестный глагол → заметка); Шаг 5 (`VERBS` + `parse_verdicts` + `apply_verdicts`); Шаг 3 проверка (8); design-renderer.md стр. 557–637
- Проблема: Парсер вердиктов на Python дублирует то, что Клод и так делает, читая сообщение владельца: `VERDICT_RE` с lookahead-ами, таблица `CYR` для кириллицы, 7 групп синонимов `VERBS` («норм», «оставляем», «голосом», «созвон», «в первую очередь»…), ветка «не понял глагол → note», отдельные глаголы для R-кодов (приоритет выше/ниже/отложить/сделано/переделать → block/status). Задача требует только «A2 → принято, B2 → текст переписан, D2 → обсудить» для решений; управление рисками кодами (`R4 — приоритет выше`) в критерии готовности нет. При этом скилл после `changed` всё равно правит `what` через Edit («переписать на „вы“»), т.е. Клод в петле обязательно.
- Правка: Заменить свободный парсер структурным вызовом, как было в черновике design-skill.md:544, но со статусами плана: `render.py verdict <код> <accepted|changed|discuss|works> [текст]` — по одному вызову на код, в одной Bash-строке через `&&`. Перевод «ок/да/норм/так не пойдёт/давайте обсудим» в статус делает Клод (он и так читает сообщение); код нормализовать одной строкой `.upper().translate(CYR)` (оставить — дёшево и нужно для голосового «А2»). Логика в рендерере ~25 строк: найти код в `decisions[]` (иначе одна строка `FORGE-GUIDE: ⚠ код Z9 не найден в гайде`, exit 0); `status`=аргумент; для `changed` — `was`←старый `what`, `what`←текст (Клод передаёт текст УЖЕ на «вы» — второй проход через Edit не нужен); `fire=False` для всех, кроме `discuss`; `verdict={date, text}`; `pending_changes.append(f"{today}: {code} — {LABEL}")`; `updated_at`; save + `do_render(jp)`; печать `FORGE-GUIDE: A2 → принято`. Убрать `VERBS`, `VERDICT_RE`, `parse_verdicts`, ветку «неизвестный глагол → note» и R-глаголы up/down/deferred/works по рискам (не по задаче; если владелец попросит поднять приоритет риска — Edit `block` в последнем `vX.Y.json` + `render`, либо отдельная задача).

Правки по плану: Стыки :27 — переписать под `verdict <код> <статус> [текст]` (убрать разделители/R-глаголы/неизвестный глагол); Шаг 5 :91 — «`VERBS` + `parse_verdicts` + `apply_verdicts`» → `apply_verdict(data, code, status, text, today)`, проверку с `parse_verdicts(...)` заменить на `render.py verdict B2 changed "…" && render.py verdict А2 accepted` → статусы в JSON + «⚠» на Z9; Шаг 3 проверка (8) — «статусы/текст/🔥/кириллица кода/неизвестный код/неизвестный статус → exit 0 + ⚠», без «приоритет/неизвестный глагол»; Шаг 7 бриф и grep-проверки — `verdicts "<текст>"` → `verdict <код> <статус> [текст]`, из description убрать триггер «R4 — приоритет выше»; Шаг 8а текст хука — «переведи его слова в статус и примени `render.py verdict <код> <accepted|changed|discuss|works> [текст]` по одному на код»; Шаг 9 D5 — замена `answer` → `verdict` (не `verdicts`); design-integrator.md:28–29 и CLI-список в Стыках :25 (`verdicts <текст…>` → `verdict <код> <статус> [текст]`).

### (pragmatist) Живой Chrome в наборе тестов: проверка (15б) запускает `/Applications/Google Chrome.app/…` headless с `--virtual-time-budget=5000`, ждёт файл до 60 с и гасит пр
- Где: План: Шаг 3 проверка (15) «с Chrome → файл начинается с `%PDF`»; Шаг 5 (`do_pdf`: Popen + ожидание + terminate); design-renderer.md стр. 245–255 и 645–694
- Проблема: Живой Chrome в наборе тестов: проверка (15б) запускает `/Applications/Google Chrome.app/…` headless с `--virtual-time-budget=5000`, ждёт файл до 60 с и гасит процесс (план сам фиксирует: «Chrome 152 сам не выходит»). Тесты хуков гоняются 6 сьютами в Шаге 12, в finishing перед мержем и в Чекпоинте C — каждый прогон поднимает браузер и тянет Google Fonts из сети. PDF при этом и так проверяется глазами на Чекпоинтах A (макет) и B (п.4 `open docs/guide/guide-v1.1.pdf`).
- Правка: Правка теста (Шаг 3, проверка (15), design-renderer.md :250–254): по умолчанию оставить только ветку `FORGE_CHROME=/nonexistent/chrome render pdf` → «Chrome не найден — PDF пропущен», exit 0, `docs/guide/guide-v1.0.pdf` нет, HTML на месте. Ветку с живым Chrome не удалять, а спрятать за опт-ин `[ "${FORGE_TEST_PDF:-}" = 1 ] && [ -x "$CHROME" ]`, и перед `render pdf` вырезать сетевую ссылку из собранного HTML (`sed -i '' '/fonts.googleapis/d' docs/guide/guide-v1.0.html`) — тогда она проверяет самое хрупкое (Popen + ожидание файла + terminate при невыходящем Chrome 152) без сети за ~4–7 с; иначе печатать `SKIP: pdf via Chrome (FORGE_TEST_PDF=1 включает)`. Синхронно поправить ожидания: план :79 (15) «с Chrome → %PDF» → «с FORGE_TEST_PDF=1 и Chrome → %PDF, иначе SKIP»; design-renderer.md Шаг 7 ПРОВЕРКА (:690) — `FORGE_TEST_PDF=1 bash …test-project-guide.sh | grep pdf`; Шаг 12 без изменений (дефолтный прогон 6 сьютов офлайн и быстрый). Живой PDF владелец видит на Чекпоинтах A и B — это уже в плане.

Дополнительно (продуктовая часть, Шаг 5 do_pdf, design-renderer.md :676–687): корень проблемы — `--virtual-time-budget` ждёт зависший fetch шрифтов, и за 60 с PDF не появляется. Добавить один повтор: если по истечении 60 с файла нет — перезапустить Chrome с `--host-resolver-rules=MAP fonts.googleapis.com ~NOTFOUND,MAP fonts.gstatic.com ~NOTFOUND` (шрифты падают мгновенно → системные шрифты, PDF за ~3 с) и печатать «FORGE-GUIDE: PDF собран без веб-шрифтов (сеть не ответила) → …». Обновить заметку риска design-renderer.md:791: «без сети/при зависшей сети — системные шрифты; зависший хост без повтора = 65 с и нет PDF».

### (architect) Нормализация кириллицы в кодах ломает R-коды: таблица CYR переводит русскую «Р» в латинскую P (по глифу), а не в R. Голосовой ввод «Р4 — приоритет выше» преврат
- Где: Шаг 5 плана (стык «Ответы кодами», строка 27) → design-renderer.md Шаг 6, строка 560: CYR = str.maketrans("АВСДОРПЕНКМТ", "ABCDOPPEHKMT") — Р→P и П→P одновременно; RISKS строка 789.
- Проблема: Нормализация кириллицы в кодах ломает R-коды: таблица CYR переводит русскую «Р» в латинскую P (по глифу), а не в R. Голосовой ввод «Р4 — приоритет выше» превратится в P4 — либо «код не найден», либо (если P4 существует) правится чужое решение. При этом стык плана обещает «кириллица в кодах нормализуется», а RISKS приложения прямо говорит, что «Р4» вместо R4 покрыто.
- Правка: В design-renderer.md Шаг 6 (:560) заменить таблицу на маппинг «по звуку русского названия буквы» (голосовой ввод — основной канал): Р→R, П→P, Н→N, Б→B, В→V, Д→D, К→K, М→M, Т→T, А→A, О→O, Е→E, С→S, Ц→C, Х→X, И→I (символы, совпадающие по глифу и звуку — А О Е К М Т Д — не меняются). В parse/apply добавить запасную попытку: если код после звукового маппинга не найден ни в by_code, ни в by_risk, пробовать глифовый вариант (Р→P, Н→H, В→B, С→C) — 3-4 строки, покрывает и набранный руками текст. Тест (8) (design-renderer.md:160, план Шаг 3) дополнить: «Р4 — приоритет выше» → f4.block=crit, «П1 — ок» → P1 accepted, «Б1 — ок» → B1 accepted (в фикстуре Lumen должна быть группа B). В стыке плана :27 вместо «кириллица в кодах нормализуется» записать правило: «кириллица в кодах маппится по звуку названия буквы (Р→R, П→P, Н→N, Б→B), с запасной попыткой по глифу». В design-skill.md:224 (и в бриф skill-creator, план Шаг 7) добавить: буквы групп под проект выбирать из набора без голосовой двусмысленности — A B D K M T (не C/S, не H/N, не V/B-пары; P/O/R заняты). RISKS design-renderer.md:789 переписать: «Р4»→R4 покрыто только после звукового маппинга.

### (architect) Ожидание теста (9) «ждут 4 решения владельца» не сходится с правилом подсчёта из того же плана. После вердиктов (8) ждут P1 (default) + D1 (discuss) = 2 решения
- Где: Шаг 3 плана (проверка (9), строка 79) и Шаг 5 (правило N); design-renderer.md Шаг 2, строка 183 `grep -q 'ждут 4 решения владельца'`; фикстура Шаг 1, строка 71 («ждут 8» = 5 + 3 — там арифметика верна).
- Проблема: Ожидание теста (9) «ждут 4 решения владельца» не сходится с правилом подсчёта из того же плана. После вердиктов (8) ждут P1 (default) + D1 (discuss) = 2 решения, плюс открытые находки с owner decision|both f2, f3, f4 = 3 → 5. merged закрывает f1 (owner code) — на счёт не влияет. Тест будет красным при правильной реализации, и исполнитель «подгонит» либо код, либо число.
- Правка: 1) План, Шаг 3 (строка 79): «summary «ждут 4 решения владельца»» → «summary «ждут 5 решений владельца»» (именно «решений» — `plural(5, 'решение', 'решения', 'решений')` даёт «5 решений», grep на «решения» тоже был бы красным). 2) design-renderer.md:183: `grep -q 'ждут 4 решения владельца'` → `grep -q 'ждут 5 решений владельца'`. 3) Чтобы исполнитель не «подгонял», добавить в Шаг 3 (или в скобках к (9)) расшифровку одной фразой: «5 = P1 (default) + D1 (discuss) + f2/f3/f4 (open, owner both|decision); f1 закрыт merged, но owner code — не считается». Чекпоинт B в плане чисел не называет — править не нужно; «ждут 5» в design-renderer.md Шаг 9 (после init: P1 + f4–f7) и в design-integrator.md:58 (после Секретаря: P1 + O1–O4) — верны каждое при своём условии.

### (architect) Противоречие с задачей и с будущей записью в decisions.yml: там сказано «разделы «как устроено» собираются механизмом карты проекта (product-mapping), не изобре
- Где: Стыки плана строка 29 («Картограф А (Flow Extractor → 01 схема, 03 путь), Картограф Б (Entity+Gap …)»), Шаг 7 бриф, Шаг 11 текст decisions ««Как устроено» — механизм product-mapping»; design-skill.md G2 строки 160-230 (полные новые промпты); product-mapping/SKILL.md:60-110.
- Проблема: Противоречие с задачей и с будущей записью в decisions.yml: там сказано «разделы «как устроено» собираются механизмом карты проекта (product-mapping), не изобретаем второй извлекатель». В плане же Картографы А/Б — новые русские промпты с другим форматом выхода (title/desc/arrow/source), никак не использующие ни промпты product-mapping (они английские, «3-7 business flows»), ни его артефакт product-map. Это и есть второй извлекатель, просто названный «Flow Extractor из product-mapping».
- Правка: Правка текстовая, в трёх местах плана; реализацию (два своих картографа) не менять.

1. Шаг 11 (decisions.yml, текст из design-integration.md D8 :201–202) — в `decision` заменить «„Как устроено“ — механизм product-mapping (bigPicture/flows/entities/gaps)» на «„Как устроено“ — два картографа по образцу product-mapping (то же деление Flow / Entity+Gap и правило „механизм для не-кодера“), но со своим выходом под разделы 01–04 гайда (about.scheme / parts / flow / roles, на „вы“, с source путь:строка); .forge/product-map.json — только подсказка». В `why` заменить «Второй извлекатель устройства проекта не нужен — product-mapping уже умеет flows/entities/gaps» на «Промпты product-mapping (английские, под интерактивный навигатор: what/benefit/howTo/inside/color) не дают ролей, источников путь:строка и текста на „вы“ — поэтому у гайда свои картографы по тому же образцу; правила языка/честности при изменении править в обоих скиллах».

2. Шаг 9 (CLAUDE.md, текст из design-integration.md D5 :129) — та же замена: «разделы „как устроено“ дают два картографа по образцу product-mapping» вместо «даёт механизм product-mapping (bigPicture/flows/entities/gaps)». Заодно в той же строке Шага 9 для `skills/product-mapping/SKILL.md` расширить добавляемую строку: «документ для владельца/читателя со стороны — `/forge:guide`; его картографы А/Б повторяют Subagent 1/2 этого скилла — при правке правил языка меняй оба».

3. Задача `.forge/tasks/2026-09-06-project-guide.md`, «Доработано на разборе» п.1 — одной строкой поправить: «по образцу product-mapping (два картографа: потоки / части+дыры), не его вызов; product-map.json — подсказка», убрать «(bigPicture / flows / entities / gaps)». Либо, если задачу не трогаем, добавить в «Стыки» плана явный пункт: «Картографы — свои промпты по образцу product-mapping, не вызов скилла и не его схема; в decisions.yml/CLAUDE.md писать именно так».

Общий файл промптов `skills/product-mapping/prompts/*.md` НЕ заводить: промпты product-mapping лежат inline в SKILL.md, вынос — рефакторинг чужого скилла вне задачи (surgical changes). Достаточно перекрёстной пометки из п.2.

### (architect) Миграция пользователей 7.7.0 не закрыта на уровне памяти: `init` удаляет `.forge/status-report.json`, но в их `index.yml` остаётся catalog-запись `status-report
- Где: Шаг 11 плана (index.yml только этого репо); Шаг 7 бриф (нет пункта про catalog у пользователя); Шаг 8а (elif подсказка — только про файл); Стыки строка 22 (данные — только vX.Y.json, статического пути нет).
- Проблема: Миграция пользователей 7.7.0 не закрыта на уровне памяти: `init` удаляет `.forge/status-report.json`, но в их `index.yml` остаётся catalog-запись `status-report: path: .forge/status-report.json` — L0 будет каждую сессию указывать на несуществующий файл, forge-context при совпадении тегов (report, what-next, findings) попытается его прочитать. План правит index.yml только этого репо (Шаг 11). Плюс новая запись `guide: path: .forge/guide/` указывает на директорию, а forge-context загружает L1 «по path» как файл.
- Правка: 1) В бриф Шага 7 (строка 125 плана) добавить явный пункт: «Шаг 1 скилла (память + миграция), сразу после `render.py init`: заменить в `.forge/index.yml` старую catalog-запись отчёта (ключ `status-report:`, три строки) на блок `guide: {path: .forge/guide/, tags: [guide, report, what-next, findings, decisions-pending, owner, risks, versions]}` через Edit; нет ни того ни другого → допиши последней записью catalog; затем `wc -c` ≤2500 с той же фразой, что в черновике :318» — перенести из G3 Шага 4 в начало, чтобы закрыть окно между unlink и правкой catalog.
2) Ослабить проверку Шага 7 (строка 127) и design-integrator.md:35: вместо `status-report` в grep запретить `status-report\.json` (путь), а ключ `status-report:` в тексте шага про catalog разрешить; в свипе Шага 9 (строка 152) добавить исключение `skills/project-guide/SKILL.md:.*status-report:` (или требовать на этой строке слово «старый отчёт», которое свип уже пропускает).
3) Опционально: в design-renderer.md do_init после unlink печатать вторую строку `FORGE-GUIDE: замени в .forge/index.yml запись catalog старого отчёта на guide:` — страховка, если скилл оборвётся. Путь `.forge/guide/` оставить как есть (директорию Клод читает через ls, ничего не падает).

### (architect) PDF версии расходится с её HTML: verdicts/merged перерисовывают `docs/guide/guide-vX.Y.html` + `guide-latest.html` последней («живой») версии, но `guide-vX.Y.pd
- Где: Шаг 5 плана (merged/link/summary «save + do_render (обе HTML)»), Шаг 8а (хук: `verdicts` «сам перерисует guide-latest.html»), Шаг 8в (finishing: только `merged`); стык «PDF только через render.py pdf».
- Проблема: PDF версии расходится с её HTML: verdicts/merged перерисовывают `docs/guide/guide-vX.Y.html` + `guide-latest.html` последней («живой») версии, но `guide-vX.Y.pdf` не пересобирают (pdf — только отдельным режимом; finishing и текст хука его не зовут). После «A2 — ок» или мержа в git лежит PDF v1.0 со старыми статусами рядом с HTML v1.0 с новыми, а критерий готовности требует «guide-v1.0.html (и PDF той же версии)».
- Правка: Принять вариант (а) — PDF = снимок сборки, Chrome в finishing/verdicts не тянуть (вариант (б) на Linux-ПК без Chrome всё равно тихо пропустит PDF и расхождение останется). Конкретно: 1) Стыки плана (после строки 26 «Версии»): «`guide-vX.Y.pdf` — снимок на момент сборки (скилл: `render` → `pdf`); `verdicts`/`merged`/`link` перерисовывают только обе HTML, PDF не трогают; после ответов и мержей истина — HTML (guide-latest.html)». 2) Шаг 4/5 (render_html, футер — design-renderer.md:489 последняя строка): условие хвоста «· обновлён {updated_at}» заменить с `если stale_tasks` на `если pending_changes или stale_tasks` (updated_at ≠ meta.date), текст: «· обновлён {updated_at}: {N} изменений после сборки (PDF v{ver} их не содержит — пересобрать: «собери гайд»){, устарел на M задач если stale}». 3) Шаг 4 тесты, группа (8) verdicts: после `verdicts "A1 — ок"` `grep -q 'PDF v1.0 их не содержит' docs/guide/guide-v1.0.html`, а до verdicts/после bump — строки нет. 4) Шаг 8а текст хука: «он сам перерисует docs/guide/guide-latest.html (PDF версии не трогает)»; Шаг 8в finishing: фраза владельцу без обещания PDF. 5) Шаг 10 D6 п.5 «Версии»: «После мержа/ответа кодом — последний vX.Y.json + guide-vX.Y.html + guide-latest.html; PDF версии — снимок сборки, не перестраивается, в футере HTML видно число изменений после сборки». 6) Чекпоинт B п.2 добавить: «`git status --short docs/guide/guide-v1.0.pdf` → пусто, в футере guide-v1.0.html появилось «обновлён … PDF v1.0 их не содержит»».

### (architect) Модель версий без черновика не защищена от оборванной сборки: `bump` создаёт vX.(Y+1).json ДО работы 7 субагентов и Edit; если сборка прервётся (лимит контекста
- Где: Стыки плана строка 22 («Отдельных current/latest/draft.json нет … версия живая до следующей сборки»), Шаг 5 (`do_bump`), Шаг 7 бриф (режим СОБРАТЬ: «дальше bump»).
- Проблема: Модель версий без черновика не защищена от оборванной сборки: `bump` создаёт vX.(Y+1).json ДО работы 7 субагентов и Edit; если сборка прервётся (лимит контекста, стоп владельца, падение агента), «последней» останется полупустая версия без HTML/PDF, и следующее «собери гайд» сделает `bump` ещё раз — появится призрачный номер, который по правилу «старые vX.Y не правятся» уже не чинится, а `render`/summary/finishing будут работать с недособранным JSON.
- Правка: Маркер «сборка завершена» — внутри JSON, а не по наличию HTML (HTML недособранной версии создаст любой `merged`/`link`/`verdicts`).

Шаг 4/5 (render.py):
- `do_init` и `do_bump` пишут в новую версию верхнеуровневое `"built_at": null` (рядом с `updated_at`, design-renderer.md:13/:318/:547).
- Режим CLI `render` БЕЗ аргументов (финальный шаг сборки скилла; он же в «добавь снимки») перед рендером ставит `data["built_at"] = today` и `save`. Внутренние `do_render` из `merged`/`link`/`verdicts` `built_at` не трогают.
- `do_bump`, сразу после `data = load(src)` (чтобы битый JSON по-прежнему давал «повреждён» — тест 14): `if not data.get("built_at"): print(f"FORGE-GUIDE: версия v{prev} ещё не собрана — продолжаю её: правь {src} и запусти render.py render"); return` — новый файл не создаётся.
- `summary`: если `built_at` пуст — дописать «(сборка не завершена — скажи «собери гайд», продолжу)»; так session-start сам напомнит про оборванную сборку.

Шаг 3 (тест 7, design-renderer.md:149-155): после первого `bump` → повторный `render bump` даёт «ещё не собрана» и `[ ! -f .forge/guide/v1.2.json ]`; затем `render render >/dev/null` (появляется `guide-v1.1.html`, `built_at` заполнен); только потом `bump major` → v2.0. Плюс одна проверка: `merged` на несобранной версии `built_at` не ставит.

Шаг 7 (бриф skill-creator), режим СОБРАТЬ: «`bump` вызывать после возврата 7 агентов, прямо перед Edit JSON; если `bump` ответил «ещё не собрана — продолжаю её» — новую версию не заводишь, дозаполняешь тот же файл и заканчиваешь `render` + `pdf`». Режим ПОКАЗАТЬ: если summary говорит «сборка не завершена» — предложить «собрать гайд» одной строкой.

Стыки плана: :22 добавить «`built_at` ставит только `render` без аргументов; версия с пустым `built_at` считается несобранной»; :25 в описании `bump` — «отказ, если последняя версия не собрана (`built_at` пуст) — скилл продолжает её».

Опционально (не обязательно): live-путь `do_render` при `built_at` пустом не перезаписывать `guide-latest.html` — чтобы «мержим» не увёз недособранный гайд на GitHub как последний.

### (architect) Папка витрины `docs/guide/` — слишком общее имя для чужого проекта, а backup.sh делает `git add docs/guide` целиком: если у пользователя уже есть docs/guide (ти
- Где: Стык плана строка 34 (backup.sh `paths+=(docs/guide)`), Шаг 8д, решение задачи «Папка версий — docs/guide/».
- Проблема: Папка витрины `docs/guide/` — слишком общее имя для чужого проекта, а backup.sh делает `git add docs/guide` целиком: если у пользователя уже есть docs/guide (типичное имя для Docusaurus/MkDocs-раздела), его несвязанные правки поедут в коммиты «[forge] память» и в push без его ведома. Тест (13) проверяет только stray.txt вне папки.
- Правка: Папку `docs/guide/` оставить (явное решение владельца, задача стр. 19), но в backup.sh добавлять только свои файлы, раскрывая маски шеллом в конкретные пути (глоб в pathspec git без совпадений роняет и add, и commit):

Стыки стр. 34 и Шаг 8д (и D4 в design-integration.md) заменить на:
```bash
git add .forge >/dev/null 2>&1 || true
paths=(.forge)
# Витрина гайда — только свои файлы; чужой docs/guide/ (VitePress/Docusaurus) не трогаем
shopt -s nullglob
for p in docs/guide/guide-v*.html docs/guide/guide-v*.pdf docs/guide/guide-latest.html; do
    git add -- "$p" >/dev/null 2>&1 && paths+=("$p")
done
shopt -u nullglob
```
`diff --cached` / `commit` / `reset` — по `"${paths[@]}"` (все элементы — существующие пути; `guide-latest.html` без глоб-символов отсеется через `&&`, если файла нет).

Тест (13): добавить чужие файлы внутри папки — трекнутый `docs/guide/intro.md` с правкой после первого коммита и новый `docs/guide/faq.md`; ожидания: в `git show --name-only HEAD` есть `guide-v1.0.html` и `guide-latest.html`, нет `intro.md`/`faq.md`/`stray.txt`; вместо `[ -z "$(git status --short -- docs/guide)" ]` — `[ -z "$(git status --short -- docs/guide/guide-v1.0.html docs/guide/guide-latest.html)" ]` и `git status --short -- docs/guide | grep -q intro.md`.

Тексты: Стыки стр. 36 и memory-backup/SKILL.md:46 («в тот же коммит попадает docs/guide/») → «в тот же коммит попадают только файлы гайда `docs/guide/guide-v*.html|pdf`, `guide-latest.html`; остальное содержимое docs/guide/ не трогается»; аналогично journal/decisions в D8 («backup.sh коммитит docs/guide» → «файлы гайда в docs/guide»).

### (architect) Напоминание при старте сессии перестаёт бывать тихим: summary считает `default`-решения ждущими, а P1 «Правило дефолта» заводится в каждом проекте всегда — посл
- Где: Шаг 5 плана (N = decisions со status default|open|discuss…), «Открытые вопросы: нет (… «дефолт»-решения считаются ждущими ответа в напоминании)», Стыки строка 35 (P1 обязателен).
- Проблема: Напоминание при старте сессии перестаёт бывать тихим: summary считает `default`-решения ждущими, а P1 «Правило дефолта» заводится в каждом проекте всегда — после первого `init` любой проект на каждой сессии (startup/resume/clear/compact) получает «ждут 1 решение владельца», пока владелец не скажет «P1 — ок». В 7.7.0 тишина «когда нечего» была свойством системы (и остаётся в тесте (9) — но там все decisions принудительно переводят в accepted).
- Правка: Вариант A (рекомендуемый, согласуется с эталоном и design-skill.md:546): в Шаге 5 разделить наборы — для стрелки summary `open_decisions` = decisions со status `open|discuss` + findings `open` с owner `decision|both` (без `default`); счётчики рендера `waiting`/`fire`/stdout «ждут ответа N» (тест (1)) оставить как есть — там default уместен как «наши предложения». Правки по тексту плана: :16 «дефолт-решения считаются ждущими ответа в напоминании» → «дефолты действуют (P1), в напоминании не считаются; в HTML остаются как «ждут ответа»»; :91 «N = decisions со status default|open|discuss» → «open|discuss»; design-renderer.md:504 `open_decisions` — отдельное множество без default; пересчитать ожидаемые числа: design-renderer.md:71 (summary по фикстуре), тест (9) :183 «ждут 4 решения», :733 и Чекпоинт B «ждут 5» → минус дефолты (P1 и другие default-решения Секретаря); тест (9)/(4) на тишину оставить — теперь они проверяют реальный случай.

Вариант B (минимальный, если решение плана :16 оставлять): `init` заводит P1 со `status: "accepted"`, `source: "плагин forge"` (это правило процесса, а не вопрос владельцу; design-skill.md:225 допускает accepted при подтверждении в памяти — правило зашито в плагин). Тогда после init summary молчит, но остальные `default`-решения Секретаря (A1, B1 …) по-прежнему считаются ждущими, и шум возвращается после первой полной сборки — противоречие со смыслом «дефолта» остаётся. Обязательно в любом варианте: обновить design-renderer.md:779 (OPEN) — вопрос закрыт выбранным вариантом.

### (advocate) «R2 — ок» превращает риск в «сделано»: находка получает status done + done_at, зачёркивается в 06 и выпадает из счёта «крит». Владелец-не-кодер, отвечая «ок» на
- Где: План, Стыки :27 («ок/сделано → done» для R<n>); design-renderer.md Шаг 6 apply_verdicts: `elif verb in ("works", "accepted"): f["status"], f["done_at"] = "done", today`
- Проблема: «R2 — ок» превращает риск в «сделано»: находка получает status done + done_at, зачёркивается в 06 и выпадает из счёта «крит». Владелец-не-кодер, отвечая «ок» на риск, обычно соглашается с «Предлагаем», а не сообщает, что уже починил. Документ сам провоцирует такой ответ: 05 говорит «Ждём по каждому: ок / переделать / обсудить», howto — «Каждое решение и риск имеет код… Отвечайте списком кодов — так / не так».
- Правка: 1. План, «Стыки» :27 — заменить «ок/сделано → done» на: «сделано/готово/работает → done + done_at (как после мержа); ок/да/принято/согласен → `agreed` — статус НЕ меняется, у находки `agreed_at: <дата>`, в чат «R2 → предложение принято», в pending_changes «{дата}: R2 — предложение принято»».

2. design-renderer.md Шаг 6 (план Шаг 5), apply_verdicts, ветка by_risk: разбить `elif verb in ("works", "accepted")` на две:
   `elif verb == "works": f["status"], f["done_at"] = "done", today`
   `elif verb == "accepted": f["agreed_at"] = today` (статус open остаётся, `fire` у находки — снять, если был).
   Для метки в said/pending_changes: `label = "предложение принято" if code in by_risk and verb == "accepted" else ("сделано" if code in by_risk and verb == "works" else LABEL[verb])` — чтобы «R2 → принято» не читалось как «сделано» и changelog не противоречил карточке.

3. Тест test-project-guide.sh, группа (8) (план Шаг 3): добавить в строку verdicts «R5 — ок» и «R6 — сделано» и проверить: у f5 `status` = `open` и есть `agreed_at`; у f6 `status` = `done` и есть `done_at`; в `$out` есть «R5 → предложение принято»; счётчик `len(pending_changes)` скорректировать (5 → 7). После `render` карточка R5 не зачёркнута, R6 — зачёркнута (`risk … done`).

4. Howto в render_html (design-renderer :489, план Шаг 4): пример расширить — «A2 — ок. B2 — переделать: …. D2 — обсудить. R4 — приоритет выше. R7 — сделано»; после легенды одна фраза: «По рискам: ок — согласны с мерой (риск остаётся в карте), приоритет выше / ниже, отложить, сделано — только когда действительно закрыто; после мержа задачи риск закрывается сам». Текст `.gt` раздела 06 — «Меры — наши предложения, отвечайте по кодам: ок / приоритет выше / ниже / отложить / сделано».

5. design-integrator.md:29 и бриф SKILL.md (план Шаг 7, таблица ОТВЕТЫ КОДАМИ): «R-коды: приоритет выше/ниже/отложить; ок — согласие с мерой, статус не меняется; сделано → done». Опционально: после «R<n> — ок» Клод предлагает «скажи «бери R<n>» — заведу задачу» (хэндофф `card:<id>` уже есть в плане).

6. `.forge/guide` формат (docs/project-guide-format.md, план Шаг 10) — упомянуть поле находки `agreed_at?` рядом с `done_at?`.

### (advocate) Ответ без тире вообще не распознаётся: «A2 ок», «D2 обсудить», строчные «a2 — ок» дают пустой список — verdicts печатает пустую строку «FORGE-GUIDE: », статусы 
- Где: design-renderer.md Шаг 6 VERDICT_RE (`\s*[—–\-:]+\s*` обязателен, флага re.I нет); design-skill.md G3 Шаг 8 таблица «A2 — ок» / «A2 ок» / «A2 — да»; G2 Шаг 0 правило режима «код вида [A-Z]\d+ с тире/двоеточием»
- Проблема: Ответ без тире вообще не распознаётся: «A2 ок», «D2 обсудить», строчные «a2 — ок» дают пустой список — verdicts печатает пустую строку «FORGE-GUIDE: », статусы не меняются, а владелец думает, что ответил. При этом черновик скилла сам обещает форму «A2 ок».
- Правка: Шаг 5 плана / design-renderer.md Шаг 6: (а) `VERB_ALT = "|".join(re.escape(k) for keys,_ in VERBS for k in keys)`; разделитель `SEP = rf"(?:\s*[—–\-:]+\s*|\s+(?={VERB_ALT}))"` — пробел допустим только перед известным глаголом (чтобы упоминание кода внутри заметки «…кнопка B2 сверху» не рвало разбор); `VERDICT_RE = re.compile(rf"(?<![\w])({CODE}){SEP}(.*?)(?=[\s.;,]*(?<![\w]){CODE}{SEP}|\s*$)", re.S | re.I)` — тот же SEP в lookahead, иначе «A2 — ок. B2 ок» продолжит проглатывать B2; `.upper().translate(CYR)` уже есть — с re.I заработает для «a2»/«а2». (б) В `apply_verdicts`: `parsed = parse_verdicts(text); if not parsed: warn.append("не нашёл ни одного кода вида «A2 — ок» в: «" + text[:80] + "»"); return [], warn` — и `updated_at`/`save`/`do_render` при пустом разборе не трогать. (в) Проверка Шага 5: в ручной прогон добавить 'A2 ок. B2 ок' → оба accepted, 'a2 — ок' → A2; в тест-группу (8) Шага 4 — кейс «A2 ок» → статус accepted и кейс «привет» → stdout содержит «⚠ не нашёл ни одного кода», JSON не изменён. (г) Шаг 7 (бриф skill-creator) и design-skill.md:97: правило режима ОТВЕТЫ КОДАМИ — «код вида A2 (любой регистр, кириллица допустима) + тире/двоеточие/пробел + слово-вердикт»; таблицу :378 оставить как есть (она уже обещает «A2 ок»). (д) Стыки (план:27): «разделители — – - : или пробел перед словом-вердиктом; ничего не распознано → ⚠, статусы не трогаются».

### (advocate) Уточнение после глагола без двоеточия молча выбрасывается: «A2 — да, только не 10 минут, а 15» → accepted, verdict.text = «принято», хвост «только не 10 минут, 
- Где: design-renderer.md Шаг 6 parse_verdicts: `note = rest.split(":", 1)[1].strip() if ":" in rest else ""`; apply_verdicts `d["verdict"] = {"text": note or LABEL[verb]}`
- Проблема: Уточнение после глагола без двоеточия молча выбрасывается: «A2 — да, только не 10 минут, а 15» → accepted, verdict.text = «принято», хвост «только не 10 минут, а 15» нигде не сохранён; «A2 — ок в целом, но давайте обсудим цену» → accepted. Для голосового ввода без двоеточий это основной сценарий.
- Правка: Шаг 5 плана / design-renderer.md Шаг 6, parse_verdicts: после совпадения ключа `k` считать `note = re.sub(r'^[\s,:—–\-]+', '', rest[len(k):])` (хвост после глагола; двоеточие больше не обязательно), а `rest` (слова владельца целиком, без концевой пунктуации) возвращать четвёртым элементом или хранить рядом. Перед присвоением verb — проверить хвост на ключи «сильных» других групп (changed: переделать/не так/иначе; discuss: обсудить/обсудим/голосом/созвон; deferred: отложить/не делаем) с границей слова `(?<![а-я])k(?![а-я])`: нашёлся ключ чужой группы → verb=None, note=rest, warn «{code}: смешанный ответ «{rest}» — уточни одним словом (ок / переделать: … / обсудить)», статус не менять. В apply_verdicts: `d["verdict"] = {"date": today, "text": rest}` (всегда слова владельца, LABEL — только когда rest пуст); для changed `what = note` (работает и без двоеточия: «B2 — переделать, контакт после согласия»); если verb=accepted и note непустой — доп. warn «{code}: принято с уточнением «{note}» — проверь текст решения». Обновить ожидаемые кортежи в Шаге 5 «Как проверим» и design-renderer.md:639 ('D2','discuss','голосом' вместо ''), и в тест (8) добавить два кейса: `verdicts "A1 — да, только не 10 минут, а 15"` → status accepted, verdict.text содержит «15», pending_changes содержит «15»; `verdicts "A1 — ок в целом, но давайте обсудим цену"` → status не изменился, вывод содержит «смешанный»/«уточни».

### (advocate) «B2 — не так» / «B2 — иначе» без текста: статус становится changed, чип «переделано», а `what` не тронут — читатель видит «переделано» с прежней формулировкой. 
- Где: design-renderer.md Шаг 6 apply_verdicts: `if verb == "changed" and note:` (при пустом note статус всё равно = changed); План Шаг 7 бриф — нет реакции на changed без текста
- Проблема: «B2 — не так» / «B2 — иначе» без текста: статус становится changed, чип «переделано», а `what` не тронут — читатель видит «переделано» с прежней формулировкой. Ни рендерер, ни бриф скилла не велят спросить «как переделать?». Именно такую форму («так / не так») предлагает legend эталона.
- Правка: 1) design-renderer.md Шаг 6 / план Шаг 5, `apply_verdicts`, ветка решений: перед `d["status"] = verb` добавить `if verb == "changed" and not note: warn.append(f"{code}: переделать — а как? напиши «{code} — переделать: <новая формулировка>»"); continue` (статус, fire, verdict, pending_changes не трогать). В ветке рисков заменить `elif verb == "changed" and note:` + общий else на ту же явную проверку с тем же текстом ⚠ (сейчас пустая заметка даёт бессмысленное «не понял вердикт «»»). 2) `parse_verdicts` (:584): для глагола `changed` без двоеточия брать заметкой хвост после ключевого слова — `note = rest.split(":",1)[1].strip() if ":" in rest else (re.sub(rf"^{re.escape(k)}", "", rest, flags=re.I).strip(" ,;—–-:") if v == "changed" else "")` — чтобы «B2 — не так, контакт после согласия» давало `('B2','changed','контакт после согласия')`; для остальных глаголов поведение прежнее (тест Шага 5 `('D2','discuss','')` не ломается). 3) План Шаг 3 / test-project-guide.sh проверка (8): две новые строки — `verdicts "B2 — не так"` → status остаётся `default`, `fire` true, stdout содержит `⚠ B2: переделать — а как?`, `pending_changes` не растёт; `verdicts "B2 — не так, контакт после согласия"` → status `changed`, `what` = «контакт после согласия», `was` = старое. 4) План Шаг 7 бриф (режим ОТВЕТЫ КОДАМИ) и таблица design-skill.md Шаг 8: добавить строку «B2 — не так» / «переделать» без текста → рендерер печатает ⚠ «а как?» → Клод задаёт владельцу один вопрос «B2 — как переделать? Сейчас: <what>», после ответа зовёт `verdicts "B2 — переделать: <его слова>"` и дальше как по строке «переделать: <текст>» (Edit what на «вы», запись в decisions.yml). 5) Опционально — в подсказку хука (план Шаг 8а, хвост напоминания) дописать «если рендерер ответил ⚠ «а как?» — спроси владельца одним вопросом».

### (advocate) Неизвестный ответ («А2 — ну не знаю») даёт только служебную строку «не понял вердикт … записал как заметку, статус не менял»; в брифе Шага 7 нет, что Клод говор
- Где: План Шаг 5 (verdicts, «неизвестный глагол → статус не меняется, заметка»); План Шаг 7 бриф; design-renderer.md Шаг 6 warn «{code}: не понял вердикт «{note}»»
- Проблема: Неизвестный ответ («А2 — ну не знаю») даёт только служебную строку «не понял вердикт … записал как заметку, статус не менял»; в брифе Шага 7 нет, что Клод говорит владельцу дальше — не-кодер останется с молчаливым «заметка» и не поймёт, что решение всё ещё висит.
- Правка: В бриф Шага 7 (режим ОТВЕТЫ КОДАМИ) добавить правило реакции на вывод рендерера: если в выводе `verdicts` есть «не понял вердикт» по коду X — Клод трактует это как «обсудить» по этому коду: в чат 2–3 варианта + своя рекомендация одной строкой, по одному коду за ход; «не знаю» / «хз» / «на твоё усмотрение» = «бери мой вариант» → после «ок» владельца применить `verdicts "X — ок"` (или «переделать: …») и записать в decisions.yml; голую фразу «записал как заметку» владельцу не показывать. Правило «не знаю = твой вариант» из строки «D2 — обсудить» таблицы G3 Шаг 8 вынести в общее правило режима (действует и при явном «обсудить», и при непонятом вердикте), а строку «Несколько кодов» дополнить: непонятые коды перечисляются отдельно («A2 — не понял, предлагаю: …»), а не прячутся за «Принял: …». В «Как проверим» Шага 7 добавить `grep -c 'не понял' forge-plugin/skills/project-guide/SKILL.md` → ≥1. Рендерер не трогать (тест (8) ждёт «не понял» и статус без изменений). Опционально, отдельной мелочью: в VERBS искать глагол не только в начале фразы (re.search вместо re.match), чтобы «не уверен, давай обсудим» становилось `discuss`.

### (advocate) Битый JSON последней версии — тупик для не-кодера: подсказка «скажи «собери гайд», пересоберу» не работает — bump грузит битый файл и падает с той же строкой, i
- Где: design-renderer.md Шаг 3: do_init `if latest_json(): print(... уже есть ...)`, load → «повреждён ({path}: {e}) — скажи «собери гайд», пересоберу»; Шаг 6 do_bump `data = load(src)`; Шаг 8 summary `except Exception: return 0`; План Шаг 5, Шаг 3 п.(14)
- Проблема: Битый JSON последней версии — тупик для не-кодера: подсказка «скажи «собери гайд», пересоберу» не работает — bump грузит битый файл и падает с той же строкой, init отказывает («гайд уже есть», проверяется только имя файла), а summary при битом файле молчит (except → return 0), так что при старте сессии владелец про поломку не узнаёт. В сообщении — сырой текст Python-ошибки.
- Правка: 1) design-renderer.md Шаг 3 (:356) / план Шаг 4: текст `load()` → «FORGE-GUIDE: файл гайда повреждён ({path}: {e}) — почини JSON по строке/колонке или верни из git: git checkout -- {path}; затем render». Фразу «скажи «собери гайд», пересоберу» убрать (bump её не выполнит). `{e}` оставить — он для Клода.
2) План Шаг 7, бриф skill-creator — добавить пункт: «Если любой режим render.py ответил «повреждён»: bump/init повторно не звать; починить JSON (Read → Edit по строке из ошибки), если не выходит — `git checkout -- <файл>` (память в git) и заново применить ответы владельца, которых в восстановленном файле нет; владельцу — одна строка «файл гайда был повреждён, восстановил», без текста ошибки и без вопросов».
3) План Шаг 5 / design-renderer.md Шаг 8 (:706-708): в summary `except GuideBroken: print(f"📖 Гайд по проекту: файл {jp} повреждён — скажи «почини гайд», восстановлю из git"); return 0` вместо молчания. Соответственно тест (14) (design-renderer.md:240, план Шаг 3 п.(14)): убрать особую ветку `if [ "$m" = "summary" ]` — summary тоже должен содержать «повреждён»; test-session-start (5) «broken» остаётся (exit 0, валидный JSON).

### (advocate) «Открой гайд» отвечает «Снимков на этой машине может не быть — они не едут через git» — фраза перенесена из старого отчёта и теперь неверна: снимки встроены в H
- Где: design-skill.md G2 Шаг 0 (режим ПОКАЗАТЬ, строка ответа); design-integrator.md эту фразу не поправил; План Стыки :23 (data: URI), :34/:36 (docs/guide в коммит памяти)
- Проблема: «Открой гайд» отвечает «Снимков на этой машине может не быть — они не едут через git» — фраза перенесена из старого отчёта и теперь неверна: снимки встроены в HTML как data: URI, а docs/guide/ уезжает в git через backup.sh. Владелец на другой машине будет зря искать пропавшие картинки.
- Правка: В design-integrator.md (правки к design-skill.md G2 Шаг 0) и в бриф skill-creator Шага 7 добавить пункт: в режиме ПОКАЗАТЬ убрать хвост «Снимков на этой машине может не быть — они не едут через git» — снимки встроены в HTML как data: URI, а `docs/guide/` едет в git через backup.sh. Строка ответа: «Открыл гайд v{version} (собран {meta.date}; {строка summary без префикса «📖 Гайд по проекту vX.Y:», если есть})»; числа — из последнего `.forge/guide/vX.Y.json` и вывода `render.py summary` (не из `latest.json` — его по стыкам нет). Опционально, если хочется сохранить правдивое предупреждение: перенести его в режим СОБРАТЬ/«добавь снимки» в виде «снимки из `.forge/guide/shots/` не в git — на другой машине новая версия соберётся без картинок, если не положить их заново».

### (advocate) Блок «Что изменилось в v1.1» появляется только если между сборками были ответы кодами или мержи: bump кладёт в changelog[0].items лишь pending_changes, а бриф Ш
- Где: План Шаг 5 do_bump (`changelog.insert(0, {items: pending_changes})`); design-renderer.md Шаг 4 п.4 `.changes` («если версия ≠ 1.0 и items не пусты»); План Шаг 7 бриф; design-skill.md G3 Шаг 3 п.12 (`changes[]` — старая схема, интегратором не перемаплено)
- Проблема: Блок «Что изменилось в v1.1» появляется только если между сборками были ответы кодами или мержи: bump кладёт в changelog[0].items лишь pending_changes, а бриф Шага 7 не велит Клоду дописать туда, что нашли агенты (новые риски, закрытые, новые решения). Обещание плана «v1.1 с блоком «что изменилось с v1.0»» при обычной пересборке без ответов не выполнится — читатель не поймёт, что нового.
- Правка: 1) План Шаг 7, бриф skill-creator — добавить фразу: «п.12 черновика (`changes[]`) → после `bump` Клод ДОПИСЫВАЕТ (Edit, append, не перезаписывать — bump уже положил туда строки ответов/мержей из `pending_changes`) в `changelog[0].items`: `+R19 …` (новые находки), `R3 сделано (задача <slug>)` (закрытые агентом, done + done_at), `O2 снято` (dropped), переписанные/переставленные коды; не больше 10 строк, самое важное первым; у v1.0 (после `init`) changelog не трогать. Слово `changes` в шаблоне чата Шага 7 (design-skill.md:362 «1–2 фразы из changes») и в правиле версий Шага 8 (:372 «попадут в changes[]») → `changelog[0].items`». 2) Шаг 7 «Как проверим» — дописать: `grep -c 'changelog' forge-plugin/skills/project-guide/SKILL.md` ≥1; `grep -c 'changes\[\]' SKILL.md` → 0. 3) Чекпоинт B п.3 — уточнить: «в блоке «что изменилось с v1.0» есть не только `O1 — принято`, но и хотя бы одна строка от агентов (новая или закрытая находка)». 4) design-integrator.md — добавить строку в список поправок: design-skill.md :141, :272, :362, :372 `changes[]` → `changelog[0].items` (append после bump). 5) Опционально (рендерер, Шаг 4, design-renderer.md:489): при версии ≠ 1.0 и пустых `items` печатать одну строку «Ответов и мержей с прошлой версии не было; разделы обновлены по коду» вместо полного скрытия блока — чтобы читатель v1.1 не гадал, что нового; без п.1 это лишь заплатка, с п.1 — страховка.

### (advocate) Напоминание при старте сессии никогда не замолчит: «ждут N решений владельца» считает все `default`, включая P1 «Правило дефолта», которое по собственному текст
- Где: План :16 («дефолт»-решения считаются ждущими), Стыки; design-renderer.md Шаг 4 counts `open_decisions` (UNANSWERED = default|open|discuss); Шаг 8 summary
- Проблема: Напоминание при старте сессии никогда не замолчит: «ждут N решений владельца» считает все `default`, включая P1 «Правило дефолта», которое по собственному тексту ответа не требует («нет возражений — действуем и не стоим»). Не-кодер каждую сессию видит «ждут 1 решение», спрашивает какое — и получает мета-правило.
- Правка: Разделить два счётчика в `counts()` (design-renderer.md Шаг 4, :498-503): `waiting` для документа оставить по UNANSWERED (счётчик «ждут ответа» в stdout render и тест (1) «ждут ответа 5, 🔥 3» не трогать), а `open_decisions` для summary считать как: решения со status open|discuss + решения default только с fire=true + находки open с owner decision|both. Тогда напоминание согласовано с 🔥: всё, что оно считает, либо 🔥, либо открытое/«обсудить»; P1 и «дефолты без возражений» не нагоняют. Следом синхронизировать числа: design-renderer.md :71 «summary «ждут 8 решений»» → «ждут 6» (B1 default+🔥, D1, O1 + f2, f3, f4); тест (9) :183 «ждут 4» — оставить (после вердиктов (8) остаётся D1 + 3 находки = 4, теперь арифметика сходится); план :733 «ждут 5 … (P1 default + 4 decision/both-находки)» → «ждут 4 (4 decision/both-находки; P1 не считается)»; план :16 и design-integrator.md :45 — «„дефолт“-решения считаются ждущими только с 🔥»; :748 — «N = decisions со status open|discuss, default при fire=true, + findings …»; test-session-start (:770) — фикстуру строить без опоры на P1. Альтернатива на 1 строку, если владелец хочет оставить «ждём по каждому дефолту» как в эталоне: заводить P1 в `skeleton()` со status «accepted» (чип «принято», но тогда расходится с эталоном, где у P1 чип «дефолт») — хуже.

### (advocate) «Как работать с документом» в рендерере — только легенда букв + пример + `howto_extra`: нет объяснения меток 🔥 и «дефолт», чипов «принято / обсудить / переделан
- Где: design-renderer.md Шаг 4 п.4 `.howto` («легенда из groups() + пример + snapshot.howto_extra») и `<footer>… вердикты по кодам — списком в чат`; План Шаг 7 бриф (нет правила для snapshot.howto_extra)
- Проблема: «Как работать с документом» в рендерере — только легенда букв + пример + `howto_extra`: нет объяснения меток 🔥 и «дефолт», чипов «принято / обсудить / переделано / уже работает», и не сказано, КУДА отвечать читателю со стороны (у партнёра/юриста нет чата с Клодом). Футер добивает: «вердикты по кодам — списком в чат». В эталоне всё это есть. `howto_extra` пишет скилл, но в брифе Шага 7 про него ни слова.
- Правка: Правка в design-renderer.md Шаг 4 п.4 (:489) и в План Шаг 7 — три точечных изменения, без новых полей JSON: (а) `.howto` зашить в рендерер как фиксированный текст: «<b>Как работать с документом.</b> Каждое решение и риск имеет код: {легенда из groups()}. Метка 🔥 — нужно на этой неделе, без этого стоим. Метка «дефолт» — наша рекомендация: нет возражений — действуем (см. P1). Отвечайте списком кодов — так / не так / как переделать:» + `.ex` пример + хвост: `snapshot.howto_extra`, а если пусто — дефолт рендерера «Ответы — владельцу проекта в любом виде: списком кодов, голосом, в переписке; он вносит их в гайд, и следующая версия выйдет с обновлёнными статусами.». Заголовок `.week` → «🔥 Решения, без которых стоим (нужны на этой неделе)» как в эталоне :125. (б) Футер: «вердикты по кодам — владельцу проекта в любом виде» вместо «списком в чат» (строка `summary` для session-start :713/:748 «ответы — кодами в чат» остаётся — она для владельца в сессии). (в) План Шаг 7, бриф: одна фраза «`snapshot.howto_extra` — необязательная фраза на «вы» о том, что будет после ответов; не знаешь — оставь пустым, рендерер подставит свой текст». Опционально в Шаг 3 (2): grep «владельцу проекта» и «Метка 🔥» в HTML. Пояснения чипов «принято/переделано/уже работает» не добавлять — эталон их не объясняет, а `.gt` раздела 05 уже перечисляет ок / переделать / обсудить.

### (advocate) Жаргон в разделе 06 для читателя со стороны: под риском печатается «→ в работе: feat-tray-save-command» / «→ влито: <slug>» — технический slug ветки и слово «вл
- Где: design-renderer.md Шаг 4 render_risk: `<p class="slug">→ {"влито" if done else "в работе"}: {task_slug}</p>`
- Проблема: Жаргон в разделе 06 для читателя со стороны: под риском печатается «→ в работе: feat-tray-save-command» / «→ влито: <slug>» — технический slug ветки и слово «влито» (мерж) в документе для партнёра/юриста; нарушает правило «без жаргона» и правило языка скилла (стоп-слово «мерж»).
- Правка: Шаг 4 плана, design-renderer.md:442 → `out.append(f'<p class="slug" title="задача: {esc(f["task_slug"])}">→ {"уже работает" if done else "в работе"}</p>')` — slug уходит в атрибут title (в печати/PDF не виден, в браузере — по наведению, Клоду он и так доступен в vX.Y.json), «влито» → «уже работает» (термин, которым план и integration.md:201 уже называют сделанное). Синхронно поправить тест design-renderer.md:177: `grep -q 'в работе: confirm-timeout' "$LATEST"` → `grep -q 'title="задача: confirm-timeout"' "$LATEST"` (или просто `grep -q 'confirm-timeout' "$LATEST"`). Стоки stdout (`FORGE-GUIDE: f1 → задача confirm-timeout`, `сделано → «title»`) не трогать — они для Клода/владельца в чате, не для документа. Класс `.slug` в CSS (:371, план :85) оставить.

### (advocate) После ответа кодом или мержа PDF той же версии остаётся старым: verdicts/merged перерисовывают только HTML (обе), `guide-vX.Y.pdf` не пересобирается и ни одной 
- Где: План Шаг 5 («после изменения save + do_render (обе HTML)»); design-renderer.md Шаг 7 (pdf вызывается только в сборке); design-skill.md G3 Шаг 8 (в чат «guide-latest.html обновлён»)
- Проблема: После ответа кодом или мержа PDF той же версии остаётся старым: verdicts/merged перерисовывают только HTML (обе), `guide-vX.Y.pdf` не пересобирается и ни одной строкой об этом не сказано. Владелец шлёт партнёру PDF v1.1 с уже отвеченными «дефолтами» как неотвеченными.
- Правка: Механически, в рендерере (надёжнее, чем помнить в скилле): в Шаге 5 плана в `run()` для режимов `verdicts` и `merged` после `save` + `do_render(jp)` вызывать `do_pdf(DOCS_DIR/guide-v{ver}.html, DOCS_DIR/guide-v{ver}.pdf)` — без Chrome он и так печатает «PDF пропущен, есть HTML» и exit 0. Вывод режимов дополняется строкой рендерера «PDF собран → …» / «Chrome не найден — PDF пропущен». В тесте Шага 3: в начале test-project-guide.sh `export FORGE_CHROME=/nonexistent` (чтобы (8)/(9) не поднимали Chrome по 3–5 с на каждый вызов), а в (15) положительный случай запускать как `FORGE_CHROME="$CHROME" render pdf`; в (8) добавить ожидание строки «PDF пропущен» в выводе verdicts. Тексты: Шаг 7 (бриф skill-creator) — режим ОТВЕТЫ КОДАМИ в чат «guide-vX.Y.html + PDF обновлены, номер тот же» / «PDF не обновил — нет Chrome»; режим «добавь снимки» — `render` + `pdf`; Шаг 8а (хук) и Шаг 8в (finishing) — «перерисует guide-latest.html и PDF той же версии»; Чекпоинт B п.2 — добавить `git diff --stat docs/guide/` показывает и `guide-v1.0.pdf`. `link` не трогать (меняет только slug-подпись, Chrome из new-task ради этого не нужен).

### (advocate) Покрытие критерия готовности — что закрыто, что нет. (1) «собери гайд» → guide-v1.0.html+PDF, разделы 00–08, футер: Шаги 4, 5 (pdf), 7, Чекпоинт B п.1 — закрыто
- Где: Критерий готовности .forge/tasks/2026-09-06-project-guide.md :9–15 ↔ План Шаги 4–11, Чекпоинты A–C
- Проблема: Покрытие критерия готовности — что закрыто, что нет. (1) «собери гайд» → guide-v1.0.html+PDF, разделы 00–08, футер: Шаги 4, 5 (pdf), 7, Чекпоинт B п.1 — закрыто. (2) повторный запуск +0.1 / «версия 2» / все версии / latest: Шаг 5 bump, Шаг 7, ЧБ п.3 — закрыто по номерам; «устаревшее убирается» (dropped) и «сделанное уходит в работает/принято» держатся только на правиле брифа, ни тест, ни чекпоинт их не проверяют. (3) ответы кодами: Шаг 5 verdicts, Шаг 8а хук, ЧБ п.2 — закрыто, но с дырами парсера (см. выше). (4) экраны: веб — Шаг 4 render_screens + бриф 5а; Telegram — render_tgmock + бриф 5б; «нечего» — ЧБ п.1. Живьём проверяется ТОЛЬКО «нечего»; веб/tg — лишь фикстурой на ЧА. (5) язык на «вы», шапка: Шаги 2, 4, 7, eval `outsider_language_vy` (Шаг 9) — механической проверки нет, в чекпоинте B нет пункта «владелец читает гайд глазами: «ты»-формы, жаргон». (6) merged/summary/«открой гайд»: Шаги 5, 8а/8в, 7, ЧБ п.5, ЧC — закрыто. (7) старый формат не собирается / документация / 7.8.0: Шаги 1, 6, 9, 10, 11, ЧC — закрыто.
- Правка: 1) Шаг 2 (фикстура): добавить 7-е решение со `status: dropped` (например `A3`, `since_version: "1.0"`), проверка Шага 2 → `7 8 4 3 1.0`. Шаг 3, группа (2): дописать `&& ! grep -q '<span class="code">A3</span>' "$HTML"`; в группе (1) stdout по-прежнему ждёт «решений 6» (counts() уже исключает dropped, design-renderer.md:494) — это и есть проверка «устаревшее убирается» из критерия. Группа (7): после `bump` A3 остаётся `dropped` в v1.1.json (код занят, не печатается). 2) Проверку superseded из decisions.yml на ЧБ — убрать (там ещё нет superseded, его ставит Шаг 11). 3) Чекпоинт B, п.3, одна команда-подтверждение языка: `grep -Eiw 'ты|тебе|тебя|тобой|твой|твоя|твои|твоё' docs/guide/guide-v1.1.html | grep -v 'class="howto"'` → пусто, плюс пункт «владелец читает 00–01 глазами: понятно человеку со стороны, без жаргона» (eval `outsider_language_vy` Шага 9 остаётся как есть). 4) Снимки: в план (Стыки или ЧБ п.1) добавить одну честную строку: «веб-снимки (Playwright) и Telegram-диалог живьём не проверяются — только фикстурой на ЧА; в v1 они тоже не гонялись (journal 2026-08-xx next)»; в Шаг 11 в `index.yml` `next:` записать «прогон гайда на проекте с веб-интерфейсом (mexc/assistent) — снимки + tg_mocks». Живой прогон на mexc в рамках этой задачи — только если владелец скажет, это расширение объёма.

## Мелкое (7)

### (pragmatist) Два режима `init` и `bump` там, где хватает одного: `init` = «нет версий → создать v1.0», `bump` = «есть версии → скопировать в следующую». Оба взаимно отказыва
- Где: План: Стыки «CLI рендерера: `init` … `bump [major]`»; Шаг 4 (`do_init`), Шаг 5 (`do_bump`), Шаг 3 проверки (7) и (11) «повторный init отказывает», (14) 7 режимов; Шаг 7 бриф «первый раз `init`, дальше `bump`»; design-renderer.md стр. 326–329 и 534–538
- Проблема: Два режима `init` и `bump` там, где хватает одного: `init` = «нет версий → создать v1.0», `bump` = «есть версии → скопировать в следующую». Оба взаимно отказывают («уже есть — bump» / «гайда ещё нет — сначала init»), скилл в Шаге 0 обязан сам выбрать («первый раз `init`, дальше `bump`»), тест проверяет оба отказа, битый-JSON-цикл гоняет 7 режимов.
- Правка: Один режим `bump [major]`; `init` убрать целиком.

Код (design-renderer.md, Шаг 3 `do_init` + Шаг 6 `do_bump` → одна функция): `vs = versions()`; если `vs` пуст — `data = skeleton(today)` (+ P1) и миграция `LEGACY_JSON` как в `do_init` :332–351 (битый легаси → `GuideBroken`, файл не создаём), `ver = "1.0"` независимо от `major`; иначе — копия последней в `vX.(Y+1)` / `v(X+1).0` с `changelog.insert(0, {…items: pending_changes})`, `meta.previous`, `stale_tasks=0` (без перевода done→deferred — как уже принято в стыках :32). Печать: «FORGE-GUIDE: создан v1.0 — перенёс N находок…» / «версия X.Y → … (прошлая не тронута)».

Правки плана: Стыки :25 — убрать пункт `init`, в `bump [major]` дописать «нет версий → v1.0 (каркас + P1 + миграция `.forge/status-report.json`)»; :35 «заводит `init`» → «заводит первый `bump`»; :38 «`init`/`bump`/…» → «`bump`/…», «миграцию старого отчёта делает `init`» → «`bump`». Шаг 3: проверка (11) → «bump без версий мигрирует status-report.json → v1.0 (8 находок, [P1], старые файлы убраны)», строку «повторный init отказывает» удалить; (10) и (14) без изменений (в (14) `bump` уже есть). Шаг 4: заголовок «модель версий, bump (первая версия + миграция)…», в grep проверки `init should` → `bump should`. Шаг 5: `do_bump` — только ветка копирования (первая ветка уже в Шаге 4) либо вся функция целиком в Шаге 4, а Шаг 5 её не трогает. Шаг 7 бриф: «СОБРАТЬ — всегда `bump`, «это версия N» → `bump major`; старый отчёт «Что дальше» bump перенесёт сам», из grep проверки убрать `render.py" init`. Чекпоинт B п.1: «`bump` переносит 21 находку». design-integrator.md :25/:27/:35/:37 — то же слово. cd_repo_root :356 без изменений.

### (pragmatist) Поля схемы JSON, которые никто не читает: `since_version` (в P1, в фикстуре, в схеме — но `render_decision`/`counts`/`groups` его не используют), `note` (пишетс
- Где: План: Стыки «Схема — design-renderer.md Шаг 1»; Шаг 2 (фикстура), Шаг 4 (`P1`, `render_decision`), Шаг 5 (`apply_verdicts` → `note`); design-renderer.md стр. 42, 308–311, 409–427, 606–607, 626, INTERFACES стр. 752–757
- Проблема: Поля схемы JSON, которые никто не читает: `since_version` (в P1, в фикстуре, в схеме — но `render_decision`/`counts`/`groups` его не используют), `note` (пишется в `apply_verdicts` при неизвестном глаголе — но не рендерится, а предупреждение и так печатается строкой ⚠), `meta.logo` (рендер берёт первую букву `project`, если пусто — поле лишнее), `deferred_reason` (не печатается нигде). Это данные «на будущее», которые надо заполнять Секретарю/Клоду и держать в фикстуре.
- Правка: Убрать четыре поля целиком, точечно:
- design-renderer.md Шаг 1 (:12 `"logo"`, :42–53 `"since_version"` у 6 решений), Шаг 3 (:309 P1, :317 skeleton — убрать `logo`/`since_version`), Шаг 4 :379 — `.logo` = первая буква `meta.project` без fallback на meta.logo; Шаг 6 :607 и :626 — убрать `d["note"] = note` / `f["note"] = note`, оставить только `warn.append(...)` + `continue`, текст warn: «{code}: не понял вердикт «{note}» — статус не менял, переспроси»; INTERFACES :752 (без `logo`), :757 (без `since_version`, `note?`), :765 — убрать фразу «`since_version` фиксирует…» (история появления кода — `git log -p .forge/guide/` / дифф соседних vX.Y.json).
- План «Стыки» :27 — «неизвестный глагол → статус не меняется, строка ⚠ в stdout» (без слова «заметка»); :32 — убрать `(deferred_reason: "вынесено в O<n>")`; в строке «Отложено — N (…)» печатать заголовок + `(→ O1)` из `links`, как делает render_risk — причина видна без отдельного поля.
- План Шаг 7 (бриф skill-creator): добавить пункт «строки `FORGE-GUIDE: ⚠ …` из `render.py verdicts` пересказать владельцу своими словами и переспросить по коду» — после удаления `note` это единственный след непонятого ответа (закрывает RISKS design-renderer.md:789).
- Шаг 2 фикстура и Шаг 3 тесты не меняются: (8б) проверяет только «не понял» и статус; счётчики (1) не зависят от этих полей.
Если владелец на Чекпоинте A захочет метку «новое в этой версии» у решений — тогда `since_version` возвращается вместе с читателем (чип в render_decision при since_version == ver), но не раньше.

### (pragmatist) Полное переписывание `docs/project-guide-format.md` (8 разделов, ~30 мин, отдельные проверки на 8 заголовков `## ` и на классы эталона) — документ, который не ч
- Где: План: Шаг 10 целиком и его проверки (`grep -c '^## ' → 8`, цикл по классам эталона); design-integration.md D6 стр. 150–163
- Проблема: Полное переписывание `docs/project-guide-format.md` (8 разделов, ~30 мин, отдельные проверки на 8 заголовков `## ` и на классы эталона) — документ, который не читает ни один процесс: на него ссылаются только docstring render.py и абзац CLAUDE.md; скилл его не открывает. Содержимое дублирует то, что уже лежит в коде и эталоне: CSS/классы — в `render.py` (CSS эталона 1:1) и в `reference-vespera-guide.html`, схема JSON — в docstring/INTERFACES рендерера и в фикстуре, правила языка/снимков/кодов — в SKILL.md. Три копии одних правил разъедутся при первой же правке.
- Правка: Шаг 10 оставить, но переписать как короткую справку-указатель (~30 строк, 4–5 разделов, без требования «ровно 8 `## `»): (а) что это и для кого (читатель со стороны, на «вы»; эталон — гайд Vespera, копия `.forge/tasks/2026-09-06-project-guide/reference-vespera-guide.html`; оговорить, что каноничный CSS живёт в `skills/project-guide/render.py`, т.к. эталон в `.forge/tasks/` не входит в поставку плагина); (б) данные: `.forge/guide/vX.Y.json`, схема — docstring/INTERFACES `render.py` и образец `tests/hooks/fixtures/guide-sample.json`; (в) одна таблица кодов/статусов/версий строго по «Стыкам» плана: `P/O/R` + буквы под проект, статусы решений `default|accepted|changed|discuss|open|dropped`, находки `open|done|deferred|works`, версии +0.1 / major по слову / после 1.9 — 1.10 / старые `vX.Y.*` не правятся; (г) «HTML только через `render.py`, руками не пишется» + лимиты снимков (≤6, PNG ≤200 КБ, `.forge/guide/shots/`). Не описывать структуру 00–08 с классами и палитру — это уже в render.py и эталоне. Проверки: `grep -n 'Archivo\|IBM Plex\|\.verdict\|Что дальше\|current.json\|redo\|1200' forge-plugin/docs/project-guide-format.md` → пусто; `grep -c 'render.py\|guide-sample.json\|vX.Y.json\|reference-vespera-guide.html' …` → ≥4; `wc -l` ≤ ~40. Убрать «→ 8» и цикл по классам эталона. Оценку времени в D6 сменить с ~30 на ~10 мин.

### (pragmatist) `evals/criteria/guide.yml` — 9 тщательно сформулированных бинарных проверок с проверкой на точное число (`grep -c '^  - id:' → 9`) для системы, которая ни разу 
- Где: План: Шаг 9 (`evals/criteria/guide.yml` (переписать: 9 проверок, включая `versions_incremental`, `codes_answered_in_chat`, `outsider_language_vy`)); design-skill.md G5 стр. 433–471; design-integrator.md («9 проверок (7 старых переписать + …)»)
- Проблема: `evals/criteria/guide.yml` — 9 тщательно сформулированных бинарных проверок с проверкой на точное число (`grep -c '^  - id:' → 9`) для системы, которая ни разу не запускалась: по CLAUDE.md «evals — каркас… корпус пуст, ни одного прогона», `error-analysis.tsv` — только заголовок. Формулировки из design-skill G5 (8 проверок) + интегратор («7 старых переписать + 3 новых» = 10, а требуют 9) — уже разъезжаются в счёте.
- Правка: В плане :150 (Шаг 9) и :38 («Поправки») заменить «9 проверок, включая versions_incremental, codes_answered_in_chat, outsider_language_vy» на: «guide.yml — содержимое целиком из design-skill.md G5 (8 проверок, id как в G5), с заменами Поправок: `phase: guide`; `.forge/guide/draft.json → guide-vX.Y.json` → `.forge/guide/vX.Y.json`; docs/guide/guide-vX.Y.html оставить». В design-integrator.md:41 исправить «9 (7 старых + 3)» → «8 по G5». Число проверок нигде не фиксировать; в «Как проверим» Шага 9 добавить только: `ruby -ryaml -e 'YAML.load_file("forge-plugin/evals/criteria/guide.yml"); puts "YAML OK"'` и `grep -c 'status-report\|draft.json\|current.json' forge-plugin/evals/criteria/guide.yml → 0`. Проверку `→ 9` из design-integration.md:34 и `→ 8` из G5 не переносить в план.

### (architect) Корень проекта для снимков вычислен на одну ступень выше, чем надо: для .forge/guide/vX.Y.json `parents[1]` — это `.forge`, а не корень репо (комментарий рядом 
- Где: Шаг 4 плана («do_render … base») → design-renderer.md Шаг 4 п.6, строка 507: `base = Path(json_path).resolve().parents[1 if json_path.parent.name == "guide" else 0]`.
- Проблема: Корень проекта для снимков вычислен на одну ступень выше, чем надо: для .forge/guide/vX.Y.json `parents[1]` — это `.forge`, а не корень репо (комментарий рядом говорит «две ступени вверх» = parents[2]). Тесты пройдут только потому, что img_tag сперва пробует Path.cwd()/p — дефект дремлет до первого вызова с cwd вне корня (finishing/хук зовут через cd_repo_root, но макет `render <json> <out>` — нет).
- Правка: design-renderer.md:507 (Шаг 4 п.6 плана): заменить на `jp = Path(json_path).resolve(); base = jp.parents[2] if jp.parent.name == "guide" and jp.parents[1].name == ".forge" else jp.parent` (комментарий оставить: «.forge/guide/vX.Y.json → две ступени вверх = корень проекта; макет → папка json»). В тест (6) после проверки «existing screenshot should be embedded…» (design-renderer.md:146) добавить вызов из поддиректории с явным путём — tmp-проект не git, cd_repo_root не переходит, снимок найдётся только через base: `mkdir -p sub && (cd sub && render render ../.forge/guide/v1.0.json ../mock.html >/dev/null); grep -q '<figure class="shot"><img src="data:image/png' mock.html; check "render with explicit json path from a subdirectory should find shots via project root" $?` — RED при parents[1], GREEN при parents[2].

### (architect) Имя фазы разъезжается между слоями внутри одного плана: state.yml/statusline/скилл — `project-guide`, evals `phase: guide` и матрица `to_guide`, команда `guide`
- Где: Стыки плана строка 21 (`state.yml: phase: project-guide`; statusline case `project-guide|guide|…`), Шаг 9 (`criteria/guide.yml` `phase: guide`, матрица `to_guide`).
- Проблема: Имя фазы разъезжается между слоями внутри одного плана: state.yml/statusline/скилл — `project-guide`, evals `phase: guide` и матрица `to_guide`, команда `guide`. Прецедент в репо — короткое имя везде одинаковое: скилл project-unblocker пишет `phase: unblocker`, критерии `phase: unblocker`, команда `unblocker`. Трейсы evals по phase не сопоставятся с state.yml без ручного маппинга.
- Правка: Выбрать одно короткое имя фазы `guide` — по прецеденту unblocker (директория скилла длинная `project-guide`, имя фазы короткое, как `project-unblocker` → `unblocker`) и потому, что `guide` уже стоит в команде, evals, матрице, `index.yml` «5 guide» и манифестах. Правки в плане `.forge/plans/2026-09-06-project-guide.md`:
- «Стыки», строка 21: `state.yml: phase: project-guide` → `state.yml: phase: guide`; statusline-case → `guide|"Phase 5"|5) "📖 Фаза 5: Гайд по проекту"` (вариант `project-guide|` убрать — иначе терпимость снова разрешит двоякое написание).
- Шаг 7, строка 125 (бриф skill-creator): `state.yml: phase: project-guide` → `state.yml: phase: guide`; строка 127 (проверка): `grep -n '…\|phase: project-guide' SKILL.md` → `phase: guide`.
- Шаг 8, строка 135: `printf 'phase: project-guide\n'` → `printf 'phase: guide\n'` (совпадёт с исходным тестом `design-integration.md:60`).
- `design-integrator.md:46`: «`phase: project-guide`; … Тест statusline D2: `printf 'phase: project-guide\n'`» → `phase: guide` (снять противоречие с `:41`).
Evals `criteria/guide.yml` `phase: guide`, матрица `to_guide`/`guide`, команда `guide.md`, имя скилла `project-guide` — без изменений.

### (advocate) Первый запуск на проекте без .forge/index.yml: skeleton даёт project «Проект», audience «для владельца проекта», eyebrow/built_from пустые, project_version пуст
- Где: design-renderer.md Шаг 3 skeleton()/read_index(); План Шаг 7 бриф; design-integrator.md («Шапка — meta.audience, meta.eyebrow, meta.project_version (из index.yml, ставит init)»)
- Проблема: Первый запуск на проекте без .forge/index.yml: skeleton даёт project «Проект», audience «для владельца проекта», eyebrow/built_from пустые, project_version пустой → шапка «Проект · для владельца проекта · 2026-09-06», `<title>Проект · Гайд по проекту v1.0</title>`, футер «собрано из » (пусто). Бриф Шага 7 не велит Клоду после init/bump заполнить meta.*; интегратор пишет «шапку ставит init» — а init ставит только project_version. Пункт критерия «в шапке видно… какая версия проекта» при пустом version в index.yml не выполняется молча.
- Правка: Сузить правку до реального шва: (а) в бриф Шага 7 одну явную строку — «после init/bump Клод через Edit заполняет meta: project (как зовёт владелец), audience (для кого), eyebrow (состояние проекта простыми словами — бывший state_line), built_from (откуда собрано: «код проекта, память .forge (какие файлы), журнал за <период>» — бывшие sources.memory/period); project_version не трогать — его ставит init из index.yml»; в проверку Шага 7 добавить `grep -c 'built_from\|meta.audience\|eyebrow' SKILL.md → ≥3`. (б) В Шаге 4 (design-renderer.md п.4, футер) — защита от пустого: `built_from or "код проекта и память .forge"`, чтобы «собрано из  ·» не печаталось даже при нарушении скилла. Пункты про «Проект»-заглушку, отсутствие .forge и «критерий версии нарушен» — не править: покрыто design-skill.md:274 + design-integrator.md:37 и h1/title гайда.

