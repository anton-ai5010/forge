### CONFLICTS
- Схема JSON. Рендерер (проверен, 13 PASS): project / eyebrow / question / built_at / updated_at / stale_tasks / next_after / sources{analysts,found} / verdict{text,finding_id} / findings[{id, owner, effort, block, status, title, what, why, source, task_slug?, screenshot?, done_at?}]. Скилл (A3): project / date / eyebrow / title / lead / next_after / verdict-строка / stale_merges / findings с date, task_slug:null, screenshot:null. Доки (H2, M2): stale_merges, generated.
  => Принять схему рендерера 1:1 (она оттестирована, HTML из неё уже отрисован). Поправить: skill A3 — пример JSON и правила переписать под поля рендерера (заголовок H1 задаётся парой project+question, лид рендерер генерирует сам из sources — поле lead не пишется; вердикт — объект {text, finding_id}); skill A5 Шаг 7 и interfaces; docs H2 (тест) и M2 (map.yml/decisions.yml) — stale_tasks вместо stale_merges, built_at вместо generated/date.
- Разметка жирного в текстах. Скилл A3/A5 и interfaces предлагают `<b>…</b>` в lead/verdict/what. Рендерер экранирует всё через html.escape и превращает только `**…**` в <b> (тест 4 прямо проверяет, что сырой <b> становится &lt;b&gt;).
  => Только `**жирный**`. В SKILL.md (A3 п.6, п.8, пример JSON) заменить `<b>…</b>` на `**…**`; упоминание «допускает <b>» в interfaces убрать.
- Формат id находки: рендерер и тесты — f1, f2…; скилл A3 — f-<YYYYMMDD>-<NN>; хэндофф в чате «card:<id>».
  => Короткие id fN (f1…fN): новая находка = max(N)+1, выданный id не переиспользуется. Рендерер не меняем; в A3 заменить правило id; в хэндоффе `card:f3` читается лучше, чем `card:f-20260904-03`.
- Имя режима CLI: docs interfaces и M2 (map.yml) пишут `render.py build`; у рендерера режим называется `render` (плюс sample / merged / link / summary).
  => `render`. В M2 строка about для skills/status-report/ → «render / merged <slug> / link <id> <slug> / summary»; в CLAUDE.md/COMMANDS.md команду по имени не упоминать вовсе (там достаточно «render.py собирает HTML»).
- Напоминание в session-start.sh: рендерер (шаг 5) зовёт `python3 "$plugin_root/skills/status-report/render.py" summary` (текст «📊 Отчёт «Что дальше»: ждут N решений владельца, отчёт устарел на M задач (…)»); доки (H2) — встроенный python-heredoc с полем stale_merges и текстом «решений ждут владельца — N, устарел на влитых задач — M».
  => Вариант рендерера (один источник правды для полей и формулировок; патченный хук уже лежит в proto/hooks/session-start.sh и проверен). От доков берём идею отдельного файла tests/hooks/test-session-start.sh (интро содержит «Phase 5» и «forge:status-report»; вывод — валидный JSON; без JSON-отчёта строки нет; с 2 open decision/both + 1 done + 1 code и stale_tasks:3 → «ждут 2 решения владельца» и «отчёт устарел на 3 задачи»; всё закрыто и stale 0 → тишина; битый JSON → rc 0 и валидный JSON). Тест 10 из шага 5 рендерера (в test-status-report.sh) не добавлять — дубль.
- Строка «Phase 5 /forge:status-report» в таблице фаз session-start.sh: рендерер (шаг 5, «если не сделает кусок доки») и доки (H2) — оба претендуют.
  => Делает шаг «session-start» (один шаг, см. план). Текст — как в proto/hooks/session-start.sh: `  Phase 5   /forge:status-report — отчёт «что дальше»: что чиню, что решаешь`.
- Вставка в new-task/SKILL.md после :104: рендерер шаг 7 (метка `[card:<id>]` или `card:<id>`, абзац без номера) и скилл шаг C (пункт 9.5, только `card:<id>`).
  => Один пункт 9.5 по тексту скилла C; формат метки — только `card:<id>` без скобок (проще искать, совпадает с хэндоффом A5: «card:<id> <title>»). Шаг 7 рендерера убрать.
- .forge/.gitignore + heredoc backup.sh + memory-backup/SKILL.md:46: рендерер шаг 4 (плюс идемпотентный цикл дописывания в существующий .gitignore и тест 13) и доки D1 (только heredoc, без цикла; плюс `.playwright-mcp/` в корневой .gitignore; плюс условная строка про вызов backup.sh из скилла).
  => Шаг 4 рендерера — у всех проектов на v7.6.0 .forge/.gitignore уже существует, без цикла HTML со снимками уедет в git. Из D1 оставить только одну строку `.playwright-mcp/` в корневой .gitignore этого репо. Пункт D1.4 (status-report в список вызывающих backup.sh) не нужен: скилл backup.sh не зовёт — JSON уезжает с итогом сессии/после мержа, как вся память.
- Правки project-unblocker/SKILL.md: скилл F (строки 207, 209, 217, 245, 246, 249, 256, 260) и доки C7(2) (207, 211, 217, 245-249, 256). У C7 ошибка: «Два слоя памяти:» — это :209, а :211 — заголовок `**.forge/direction.yml**`.
  => Шаг F скилла целиком (номера строк сверены, есть правка НИКОГДА :260). C7(2) убрать.
- Строка в таблице using-forge/SKILL.md: скилл G (после :137, рядом с project-unblocker) и доки C8 (после :117, рядом с execute).
  => Одна строка после :117 (Phase 5 идёт за Phase 4). Шаг G убрать.
- Evals: скилл E и доки C7(3-5) оба создают evals/criteria/status-report.yml (6 vs 7 проверок), оба правят unblocker.yml:16 и transition-matrix.tsv одинаково.
  => Шаг E скилла (6 проверок, есть handoff_only_by_owner_word и разведение триггеров). C7(3-5) убрать.
- Замена ROADMAP.md в доках: скилл H (CLAUDE.md:90,92; COMMANDS.md:399; GUIDE.md:431; runtime-flow:163,203) полностью дублирует правки внутри docs C1/C3/C5/C6.
  => Все файлы документации правит кусок «доки» (C1–C6). Шаг H убрать. Единственное, что остаётся за скиллом в доках, — F (навигатор), E (evals), D (prompt-тесты).
- Куда Playwright MCP кладёт снимок: скилл A4 — `filename: "sr-<id>.png"` и mv с fallback (корень ИЛИ .playwright-mcp/); рендерер interfaces — проверено вживую, что MCP пишет только в корень проекта или `.playwright-mcp/` («outside allowed roots»).
  => Детерминированно: `browser_take_screenshot {type:"png", filename:".playwright-mcp/sr-<id>.png", scale:"css"}` → `mkdir -p .forge/reports/shots && mv .playwright-mcp/sr-<id>.png .forge/reports/shots/<id>.png`; после всех снимков `browser_close` и `rm -rf .playwright-mcp`. Без `rm -f ./page-*.png` и без двойного mv. В A4 заменить.
- Числа для чата (скилл A5 Шаг 7: «числа бери из строки, которую напечатал render.py render»), но `render` печатает только «FORGE-REPORT: HTML собран → путь». Docs interfaces предлагают формат `findings=N blocks=M …`.
  => Расширить режим `render` второй строкой stdout: `FORGE-REPORT: {total} находок → {blocks} блоков, чиню я — {code}, решаешь ты — {decision+both}, отложено — {deferred}, снимков — {shots}` (всё уже есть в counts(), shots = число встроенных <img class="shot">). Добавить проверку в тест (1). Скилл берёт эту строку как есть.
- state.yml после отчёта: доки H1 — «в конце скилл пишет phase: idle»; скилл A1 — пишет только в начале (открытый вопрос).
  => Пишем только в начале (`phase: status-report`), в конце ничего: следующий скилл перезапишет сам (new-task при взятии карточки). Фразу про idle из H1 убрать.
- Стартовый git-шаг: доки G1 — stash двух чужих файлов + backup.sh на master (коммит + push памяти) + ветка feat/status-report + первый коммит формата.
  => Упростить: `git stash push -m "wip tray-save-command" -- .claude/settings.json forge-tray/forge-tray-mac.py` → `git checkout -b feat/status-report` (HEAD == master, 0 коммитов расхождения) → `git add forge-plugin/docs/status-report-format.md && git commit -m "docs: формат отчёта «Что дальше»"`. Ранний backup.sh/push на master не нужен: learnings.yml и task-файлы уедут штатно при «мержим» (finishing → git add -A + backup.sh). Stash settings.json теряет лишь одноразовый allow для sync.sh — безвредно; вернуть `git stash pop` после релиза.
- Порог confidence для находок агентов (скилл: 70; critique использует 80) и заголовок Блока 2 без имени второго человека (константа «Решения — нужны от тебя»).
  => 70 и константы — как спроектировано; override заголовков блоков через JSON не заводим (нет запроса). Вопрос про имя второго человека — владельцу (см. open questions), по умолчанию v1 без него.

### GAPS
- Строки счётчиков в stdout `render.py render` нет (см. конфликт) — без неё скилл не может честно назвать числа в чате; добавить в шаг 2 и в тест (1).
- Тест на интро session-start.sh (таблица фаз) отсутствует во всём репо (разведка MISSED) — закрывается новым tests/hooks/test-session-start.sh (адаптированный H2).
- Запись catalog в .forge/index.yml чужих проектов: init.md её не создаёт (файла при init нет), M2 добавляет только в этом репо; interfaces доков говорят «скилл дописывает при первой сборке», но ни один A-шаг скилла этого не делает. Добавить в A3 (Шаг 4 «Запись данных») 3 строки: если в `.forge/index.yml` нет `status-report:` в catalog — дописать `status-report: {path: .forge/status-report.json, tags: [report, what-next, findings, decisions, owner]}` без note и проверить `wc -c .forge/index.yml` ≤ 2500.
- Критерий «Слова „что дальше по проекту“ и „статус проекта“ не перехватываются» проверяется только description-ом с NOT-for и prompt-тестом D (нужен `claude` CLI, ~2 мин) — автоматически в bash-сьюте не ловится. Живая проверка — на чекпоинте «прогон на живом проекте»: сказать «что дальше по проекту» и убедиться, что сработал unblocker.
- Скриншоты нельзя проверить на репозитории плагина (нет http-интерфейса). Для чекпоинта «живой проект» нужен проект владельца с запущенным интерфейсом — иначе ветка «снимки» до релиза не прогоняется (вопрос владельцу).
- `.forge/sketches/` в этом репо нет — render.py сам создаёт родительские папки (proto render.py:297), отдельного mkdir не нужно; но макет коммитится как память — учесть при `git status` перед мержем (ожидаемый новый файл).
- Формат-эталон forge-plugin/docs/status-report-format.md сейчас untracked (`??`) — первый коммит ветки должен его добавить (в упрощённом G1 это есть).
- Разведка MISSED про github-sync лейблы (sync.sh:82,127-131), validate.md:377, forge-graph.html — по принятому решению «на GitHub отчёт не отражается» и «фаза про весь проект, не после каждой задачи» — правок не требуют; фиксируем явно, чтобы не искали.

### WRONG FRAGMENTS
- docs C1 (CLAUDE.md): «:85 Служебный мусор (…) отсечён» — на самом деле строка 83; «:87 ## Development Workflow — 6-фазный pipeline» — строка 85 (87 — «### Phase 0»); «:127 Auto-handoff» — абзац «По умолчанию: …» на строке 126 (125 — заголовок). Подсекцию Phase 5 вставлять после :117 («Условный handoff…»), 118 — пустая, 119 — «### Git-модель».
- docs C3 (COMMANDS.md): «после :296 (`---` после примера execute)» — :296 это закрывающие ```, `---` стоит на :298, «## 7. /forge:discover» — :300. Вставлять секцию 6.5 после :298.
- docs C7(2) (project-unblocker): «:211 Два слоя памяти:» — это строка 209; :211 — «**`.forge/direction.yml`** (для Клода…)». У скилла F номера верные.
- docs interfaces + M2: режим `render.py build` не существует — режим называется `render`.
- skill A3/A5/interfaces: поля `title`, `lead`, `date`, `stale_merges`, verdict-строка и `<b>` внутри JSON — не совпадают с оттестированным рендерером (project+question, built_at, stale_tasks, verdict{text}, `**…**`); сырой `<b>` в what/why будет показан как текст `<b>` (тест 4 рендерера это фиксирует).
- skill A5 Шаг 7: «числа бери из строки, которую напечатал render.py render» — рендерер такой строки не печатает (только «HTML собран → путь»); нужна доработка шага 2.
- skill A4: `mv ./sr-<id>.png … || mv .playwright-mcp/sr-<id>.png …` и `rm -f ./page-*.png` — MCP пишет только в корень/`.playwright-mcp/`; с явным `filename: ".playwright-mcp/sr-<id>.png"` fallback и чистка page-*.png не нужны.
- docs H2: ожидания теста «ждут владельца — 2», «устарел на влитых задач — 3», поле `stale_merges`, поле `generated` — не совпадают с render.py summary («ждут 2 решения владельца», «отчёт устарел на 3 задачи», `stale_tasks`, `built_at`).
- docs H1: «в конце — echo "phase: idle" > .forge/state.yml» — скилл этого не делает (решено: пишем только в начале).
- docs G1: `bash forge-plugin/skills/memory-backup/backup.sh` на master до начала работы — лишний коммит+push master до задачи; убрано (см. конфликты).
- docs D1 п.4: строка про вызов backup.sh из status-report в memory-backup/SKILL.md:39-42 — условие не выполняется (скилл backup.sh не зовёт), не вносить.
- renderer шаг 5: «Таблица фаз … ЕСЛИ это не делает кусок документация» — неопределённость снята: строку добавляет шаг session-start (один раз).

### CUT
- skill H (ROADMAP.md → отчёт в CLAUDE.md/COMMANDS.md/GUIDE.md/runtime-flow) — полный дубль docs C1/C3/C5/C6.
- skill G (using-forge строка после :137) — дубль docs C8; оставлена одна строка после :117.
- docs C7 целиком: (1) фраза в execute/SKILL.md:236 — обновление после мержа описано в finishing, execute только передаёт; (2) дубль F; (3-5) дубль E; (6) абзац «Как генерируется в плагине» в status-report-format.md — эталон формата не про реализацию.
- docs D1 (кроме одной строки `.playwright-mcp/` в корневой .gitignore) — заменён шагом 4 рендерера с идемпотентным циклом и тестом 13.
- docs H2 встроенный python-heredoc в session-start.sh — заменён вызовом `render.py summary` (proto/hooks/session-start.sh); файл теста из H2 остаётся, ожидания — под summary.
- renderer шаг 5 тест 10 (session-start внутри test-status-report.sh) — дубль test-session-start.sh.
- renderer шаг 7 (new-task) — дубль skill C.
- docs G1: backup.sh + push master до начала работы.
- docs C3: строки «N. «мержим» — отчёт обновится сам» в сценариях «API» / «Миграция» / «Быстрая разработка» (:830/:846/:870) — сам кусок доков признаёт их необязательными; Phase 5 — не шаг задачи.
- docs C2: абзац «Обновление до новой версии: /plugin marketplace update …» в README — процедура обновления и так в релизном шаге; критерий «Фаза 5 описана в README» закрывают строка таблицы и строка в «Полезных командах».
- renderer open question: done-карточка в SAMPLE для макета — не нужна, показать владельцу словами на чекпоинте (зачёркнутая карточка с зелёным чипом «Сделано» в конце блока + «устарел на N задач» в мете).
- skill open question: подкоманда `render.py done <id>` для решений — не заводим; по «давай решения» Клод меняет `"status": "open"` → `"done"` у карточки через Edit и зовёт `render.py render`.
- skill open question: вызов backup.sh сразу после полной сборки — нет; JSON уезжает с итогом сессии / после мержа, как вся память (session-start и так напомнит про несохранённое >24ч).
- docs M2: пересчёт `files:` в map.yml для tests/ (`find … | wc -l`) — оставить, но не вычислять руками в плане; остальное M2 как есть.
- Опциональный override заголовков блоков через JSON (`blocks: {biz: {title, sub}}`) — не в v1.

### ORDERED PLAN
1. [step] (docs) Шаг 0. Git: чужой WIP в stash, ветка feat/status-report, первый коммит — формат-эталон
   Упрощённый G1. `cd /Users/mac/Projects/Plugin/plugin && git stash push -m "wip tray-save-command: settings.json + forge-tray-mac.py" -- .claude/settings.json forge-tray/forge-tray-mac.py && git checkout -b feat/status-report && git add forge-plugin/docs/status-report-format.md && git commit -m "docs: формат отчёта «Что дальше» (status-report-format.md)"`. Без backup.sh/push на master (память уедет при «мержим»). .forge/learnings.yml и два task-файла остаются грязными — это память, finishing заберёт. Как проверим: `git branch --show-current` → feat/status-report; `git stash list` → 1 строка; `git status --short` → ` M .forge/learnings.yml`, `?? .forge/tasks/…` (2 файла), без settings.json/forge-tray.
2. [step] (renderer) Шаг 1. Тест RED — forge-plugin/tests/hooks/test-status-report.sh
   Как в куске renderer шаг 1: `cp …/scratchpad/proto/tests/hooks/test-status-report.sh forge-plugin/tests/hooks/ && chmod +x`. Добавить в проверку (1) ожидание строки счётчиков из `render`: после `render render` захватить stdout в `out` и проверить `printf '%s' "$out" | grep -q "$total находок → $blocks блоков"` (числа — те же jget). Как проверим: `bash forge-plugin/tests/hooks/test-status-report.sh` → 13 FAIL, «13 test(s) FAILED» (render.py ещё нет).
3. [step] (renderer) Шаг 2. Рендерер forge-plugin/skills/status-report/render.py (GREEN) + строка счётчиков
   Как в куске renderer шаг 2: `mkdir -p forge-plugin/skills/status-report && cp …/scratchpad/proto/skills/status-report/render.py forge-plugin/skills/status-report/ && chmod +x`. Одна правка в do_render() (proto :296-299): после `print(f"FORGE-REPORT: HTML собран → {html_path}")` добавить `c = counts(data); print(f"FORGE-REPORT: {plural(c['total'],'находка','находки','находок')} → {plural(c['blocks'],'блок','блока','блоков')}, чиню я — {code}, решаешь ты — {c['open_decisions']}, отложено — {len(c['deferred'])}, снимков — {html_text.count('class=\"shot\"')}")`, где code = число findings с status==open и owner==code (добавить в counts()). Как проверим: `bash forge-plugin/tests/hooks/test-status-report.sh` → 13 PASS, «All tests passed»; из корня репо `python3 forge-plugin/skills/status-report/render.py` → «FORGE-REPORT: нет .forge/status-report.json — отчёт ещё не собирали», rc 0, файлов в .forge не появилось.
4. [step] (renderer) Шаг 3. Макет: образец → .forge/sketches/status-report-mockup.html → open
   Как в куске renderer шаг 3: `python3 forge-plugin/skills/status-report/render.py sample …/scratchpad/status-report-sample.json && python3 forge-plugin/skills/status-report/render.py render …/scratchpad/status-report-sample.json .forge/sketches/status-report-mockup.html && open .forge/sketches/status-report-mockup.html`. Образец НЕ класть в .forge/status-report.json (иначе session-start будет напоминать про Lumen). Как проверим: в браузере «Lumen: что доделать…», «Главное одной фразой», Блок 1/2/3, футер «Блок 4 «Потом» — 1 находка»; `grep -c 'class="tag' .forge/sketches/status-report-mockup.html` → 3; `ls .forge/status-report.json` → нет файла.
5. [checkpoint] (renderer) Чекпоинт A: реакция на макет
   Показываем: открытый макет (Lumen, 8 находок → 4 блока, 1 отложена). Вопросы по одному: (1) читается ли порядок и заголовки блоков «Кровь из носа — чиню в коде» / «Решения — нужны от тебя» / «Скоро — усиливает продукт»; (2) понятны ли чипы Код / Решение / Решение+код и бейдж S/M/L; (3) хватает ли футера для «Потом» и отложенного. Словами описать, чего на макете нет: после «мержим» карточка задачи станет зачёркнутой с зелёным чипом «Сделано» и уедет в конец блока, в мете появится «обновлён … · устарел на N задач». Владелец подтверждает: «вид и порядок ок» — только после этого идут скилл и точки вызова. Правки макета = правки CSS/BLOCKS в render.py + синхронно ожидания в тесте.
6. [step] (renderer) Шаг 4. HTML и снимки — не в git: .forge/.gitignore, backup.sh (heredoc + идемпотентный цикл), init.md heredoc, memory-backup/SKILL.md:46, тест 13, `.playwright-mcp/` в корневой .gitignore
   Как в куске renderer шаг 4 (5 правок + тест 13 перед «# --- (8)» в test-memory-backup.sh:182). Плюс одна строка из docs D1: `grep -qx '.playwright-mcp/' .gitignore || printf '.playwright-mcp/\n' >> .gitignore` (корень репо). Как проверим: `bash forge-plugin/tests/hooks/test-memory-backup.sh | tail -1` → «All tests passed» (14 PASS); `touch .forge/status-report.html && git check-ignore -q .forge/status-report.html && echo ignored && rm .forge/status-report.html` → ignored; `git check-ignore .playwright-mcp/x.log` → совпадение; `diff <(sed -n '36,43p' forge-plugin/skills/memory-backup/backup.sh) <(sed -n '789,796p' forge-plugin/commands/init.md)` → пусто.
7. [step] (skill) Шаг 5. Скилл forge-plugin/skills/status-report/SKILL.md (A1–A5 с правками под рендерер)
   Текст из A1–A5 с изменениями: A3 Шаг 4 «Запись данных» — пример JSON и правила под схему рендерера (project, eyebrow, question, built_at, updated_at, stale_tasks:0, next_after, sources{analysts:4, found:N}, verdict{text, finding_id}, findings[{id fN, owner, effort, block, status, title, what, why, source, task_slug?, screenshot?}]); id — fN, новый = max+1, не переиспользуется; жирный — только `**…**`; в конце Шага 4 — дописать catalog-запись status-report в .forge/index.yml, если её нет (без note, `wc -c` ≤ 2500). A4 — снимки: `browser_resize {1280,800}` → `browser_navigate` → `browser_take_screenshot {type:"png", filename:".playwright-mcp/sr-<id>.png", scale:"css"}` → `mkdir -p .forge/reports/shots && mv .playwright-mcp/sr-<id>.png .forge/reports/shots/<id>.png` → в JSON `"screenshot": ".forge/reports/shots/<id>.png"`; после всех — `browser_close`, `rm -rf .playwright-mcp`; ≤6 снимков; живой адрес — первый с curl 2xx/3xx из infrastructure.yml/README/docker-compose/.env/стандартных localhost-портов; нет — «интерфейс не запущен — отчёт без снимков». A5 Шаг 6 — `python3 "$CLAUDE_PLUGIN_ROOT/skills/status-report/render.py" render` + `xdg-open … || open …`; Шаг 7 — числа из второй строки FORGE-REPORT; Шаг 8 — хэндофф в new-task текстом `card:<id> <title>` (+ what/why контекстом); state.yml только в начале (phase: status-report). Как проверим: `head -2 SKILL.md | tail -1` → `name: status-report`; длина description ≤ 1024 (python3 split('---')[1]); `grep -c '^### Агент' SKILL.md` → 4; `grep -c '^## Шаг' SKILL.md` → 8; `grep -c 'stale_tasks' SKILL.md` ≥ 1 и `grep -c 'stale_merges' SKILL.md` → 0; `grep -c '<b>' SKILL.md` → 0; `sed -n '/^```json/,/^```$/p' SKILL.md | sed '1d;$d' | python3 -c "import json,sys;d=json.load(sys.stdin);print(d['findings'][0]['id'], d['verdict']['text'][:10])"` → `f1 …`.
8. [step] (skill) Шаг 6. Команда forge-plugin/commands/status-report.md
   Как в куске skill шаг B (frontmatter description + `disable-model-invocation: true` + «Invoke the forge:status-report skill and follow it exactly as presented to you»). Как проверим: `tail -1 forge-plugin/commands/status-report.md` → строка Invoke; `grep -c 'disable-model-invocation: true' forge-plugin/commands/status-report.md` → 1.
9. [step] (skill) Шаг 7. new-task/SKILL.md — пункт 9.5: связь карточки с задачей (render.py link)
   Как в куске skill шаг C: после :104 (`9. **Сохрани** …`) вставить пункт 9.5 с `python3 "$CLAUDE_PLUGIN_ROOT/skills/status-report/render.py" link <card-id> <slug>`; метка только `card:<id>`; без метки или без .forge/status-report.json — молча пропустить; метку в текст задачи не переносить. Как проверим: `grep -n 'render.py" link' forge-plugin/skills/new-task/SKILL.md` → одна строка между «9. **Сохрани**» и «10. **GitHub-sync»; функционально: `T=$(mktemp -d) && cd $T && mkdir .forge && R=/Users/mac/Projects/Plugin/plugin/forge-plugin/skills/status-report/render.py && python3 $R sample .forge/status-report.json >/dev/null && python3 $R link f3 refund-fee | tail -1` → «FORGE-REPORT: карточка f3 → задача refund-fee».
10. [step] (renderer) Шаг 8. finishing-a-development-branch/SKILL.md — после мержа `render.py merged <slug>` (до backup.sh)
   Как в куске renderer шаг 6: вставить блок «Отчёт «Что дальше» (Фаза 5)» перед :131 («**Затем сохрани память…»), после блока про index.yml (126-129), чтобы изменённый JSON уехал тем же коммитом backup.sh. Как проверим: `grep -n 'render.py" merged' …/finishing-a-development-branch/SKILL.md` → одна строка, номер меньше номера `grep -n 'Затем сохрани память'`; в корне репо (отчёта нет) `python3 forge-plugin/skills/status-report/render.py merged x; echo rc=$?` → пустой вывод, rc=0.
11. [step] (new) Шаг 9. session-start.sh: строка Phase 5 в интро + напоминание через `render.py summary` — сначала тест (RED), потом хук (GREEN)
   Слияние renderer шаг 5 (код) и docs H2 (тест). (а) Создать forge-plugin/tests/hooks/test-session-start.sh по каркасу H2, ожидания под summary: тест (4) кладёт `{"stale_tasks":3,"findings":[{"id":"a","owner":"decision","status":"open"},{"id":"b","owner":"both","status":"open"},{"id":"c","owner":"decision","status":"done"},{"id":"d","owner":"code","status":"open"}]}` и ждёт «ждут 2 решения владельца» и «отчёт устарел на 3 задачи»; (5) `{"stale_tasks":0,"findings":[{"owner":"decision","status":"done"}]}` → нет «📊 Отчёт»; (6) битый JSON → rc 0 и валидный JSON; (1) интро содержит «Phase 5» и «forge:status-report»; (3) без файла — нет «📊 Отчёт». Запуск до правки хука → FAIL на (1) и (4). (б) Хук: применить diff proto/hooks/session-start.sh к forge-plugin/hooks/session-start.sh — вставка `report_warn` после блока версии (перед :52), строка `  Phase 5   /forge:status-report — отчёт «что дальше»: что чиню, что решаешь` после :62, `$warning$mem_warn$report_warn` в :71 (можно `cp …/proto/hooks/session-start.sh forge-plugin/hooks/session-start.sh` — diff показал ровно эти три правки). Как проверим: `bash forge-plugin/tests/hooks/test-session-start.sh | tail -1` → «All tests passed»; в корне репо `bash forge-plugin/hooks/session-start.sh </dev/null | grep -c 'Phase 5'` → 1, `| grep -c 'ждут'` → 0.
12. [step] (docs) Шаг 10. statusline.sh — «📊 Фаза 5: Что дальше»
   Как в docs H1: после :32 (`execute|"Phase 4"|4) …`) добавить `    status-report|"Phase 5"|5) phase_icon="📊 Фаза 5: Что дальше" ;;`. Без фразы про idle. Как проверим: `d=$(mktemp -d) && mkdir "$d/.forge" && printf 'phase: status-report\ntask: отчёт\n' > "$d/.forge/state.yml" && cd "$d" && echo '{}' | bash /Users/mac/Projects/Plugin/plugin/forge-plugin/hooks/statusline.sh` → содержит «📊 Фаза 5: Что дальше».
13. [step] (skill) Шаг 11. Prompt-тесты триггеров: prompts/status-report.txt, prompts/project-unblocker.txt, run-all.sh
   Как в куске skill шаг D: два файла промптов (наивный «собери отчёт документом…» и негативный «Что дальше по проекту? Куда двигать…») + две строки в SKILLS=(…) run-all.sh после :18. Живой прогон здесь не делаем — на чекпоинте B. Как проверим: `sed -n '10,21p' forge-plugin/tests/skill-triggering/run-all.sh` содержит "project-unblocker" и "status-report"; `bash -n forge-plugin/tests/skill-triggering/run-all.sh`.
14. [checkpoint] (new) Чекпоинт B: прогон на живом проекте (скилл + точки вызова готовы, доки ещё нет)
   Как запускаем незарелиженный плагин — так же, как tests/skill-triggering/run-test.sh: в проекте владельца с `.forge/` (лучше с запущенным интерфейсом в браузере — см. вопрос владельцу) `claude --plugin-dir /Users/mac/Projects/Plugin/plugin/forge-plugin`. Показываем: (1) «собери отчёт» → 4 агента, JSON записан, HTML открыт в браузере, в чате 3–5 строк с числами из FORGE-REPORT; при живом http — снимки в карточках, в корне проекта нет `.playwright-mcp/` и PNG (`ls -a` + `git status --short`); (2) «что дальше по проекту» → сработал unblocker, не отчёт; (3) «бери» на первой карточке «Код» → new-task, после сохранения task-файла в HTML «→ в работе: <slug>»; (4) имитация мержа: `python3 …/render.py merged <slug>` → карточка зачёркнута «Сделано», мета «устарел на 1 задачу»; (5) в новой сессии интро содержит «📊 Отчёт «Что дальше»: ждут N решений владельца, отчёт устарел на 1 задачу». Владелец подтверждает: вердикт и порядок совпадают с его ощущением проекта; карточки «Решение» — действительно его вопросы (не спрятанный код); язык без жаргона. Если снимать было нечего — фиксируем, что ветка снимков проверится на первом проекте с интерфейсом после релиза.
15. [step] (skill) Шаг 12. Навигатор project-unblocker без ROADMAP.md + evals + using-forge
   Скилл F (project-unblocker/SKILL.md: 207, 209, 217, 245, 246, 249, 256, 260 — old→new как в куске skill, перед правкой `sed -n '207p;209p;217p;245p;246p;249p;256p;260p'`); скилл E (создать evals/criteria/status-report.yml с 6 binary-проверками; unblocker.yml:16 → «Обновлён .forge/direction.yml (ROADMAP.md не пишется — витрина для глаз теперь отчёт «Что дальше», /forge:status-report)?»; transition-matrix.tsv — 9 столбцов × 8 строк); using-forge/SKILL.md — одна строка `| forge:status-report | Phase 5 — отчёт «Что дальше»: что чинит Клод, что решает владелец (HTML из .forge/status-report.json) |` после :117. Как проверим: `grep -n 'ROADMAP' forge-plugin/skills/project-unblocker/SKILL.md` → ровно 2 строки (217, 260) со словами «не пишет» / «писать ROADMAP.md»; `ruby -ryaml -e 'YAML.load_file("forge-plugin/evals/criteria/status-report.yml"); puts "OK"'` → OK; `python3 -c "import csv;r=list(csv.reader(open('forge-plugin/evals/transition-matrix.tsv'),delimiter='\t'));print(len(r),{len(x) for x in r})"` → `8 {9}`; `grep -n 'forge:status-report' forge-plugin/skills/using-forge/SKILL.md` → строка 118.
16. [step] (docs) Шаг 13. Документация: CLAUDE.md, README.md, COMMANDS.md, init.md, runtime-flow.md, GUIDE.md
   C1 с исправленными номерами: CLAUDE.md :3 и :85 «7-фазный (… → 5)», :83 мусор + `status-report.html`, `reports/shots/`; :90/:92 без ROADMAP.md; подсекция «### Phase 5 — Итог: что дальше» после :117 (текст из C1, в «Выход» формулировка напоминания — «ждут N решений владельца, отчёт устарел на M задач»); :126 (Auto-handoff) — фраза «Phase 5 в цепочку auto-handoff не входит…»; таблица команд после :174; :203/:204 «7 фазам / 7 фаз». C2 без абзаца про обновление: README :3, :5 (пометка «историческая»), строка таблицы после :18, строка в «Полезных командах» после :53, :82. C3 без строк в сценариях: :9, :11, :14-16 (ASCII-схема), строка таблицы после :28, абзац «Phase 4 → Phase 5 — без auto-handoff» после :38, :287, новая секция «## 6.5. /forge:status-report (Phase 5: Итог — что дальше)» после :298, :399, workflow :791/:815 (+ пункт 12), «Команды по этапам» после :919, «Быстрая помощь» после :1016. C4: init.md :564, Phase 5 после :582, перенумерация :585-586 → 6/7, строка таблицы после :624, :660, :855; catalog-шаблон Step 8 не трогаем. C5: runtime-flow :12, :30, :117, mermaid после :126 и :156, :163, строка после :168, :187, :203, :213, timeline после :302. C6: GUIDE строка после :152, :431. Как проверим: `grep -rn '6-фазн\|6-phase\|6 фаз\|6-Phase\|ROADMAP' CLAUDE.md GUIDE.md forge-plugin/README.md forge-plugin/COMMANDS.md forge-plugin/commands/init.md forge-plugin/docs/forge-runtime-flow.md forge-plugin/evals` → пусто (кроме строк со словами «не заводится / не пишется / писать ROADMAP.md»); `grep -n '^## 6.5' forge-plugin/COMMANDS.md` → одна строка между `## 6.` и `## 7.`; `grep -n '### Phase 5' CLAUDE.md` → между Phase 4 и «### Git-модель»; `sed -n '584,590p' forge-plugin/commands/init.md` показывает `6. /forge:sync`, `7. /forge:validate`.
17. [step] (docs) Шаг 14. Версия 7.7.0 в трёх манифестах + память проекта (.forge)
   M1 — одна sed-команда по plugin.json, forge-plugin/.claude-plugin/marketplace.json, .claude-plugin/marketplace.json (7.6.0 → 7.7.0; «6-phase … execute)» → «7-phase … execute → status-report)»; «Forge — 6-phase» → «Forge — 7-phase»). forge-tray/forge-tray-mac.py не трогать (в stash). M2 — .forge/index.yml из …/scratchpad/index-candidate.yml (2188 байт; :3 «7-фазный … → 5 status-report», version 7.7.0, catalog status-report без note, note у direction убран, session-блок коротко); map.yml (commands 27, skills 35, блок skills/status-report/ с about «render / merged <slug> / link <id> <slug> / summary», docs about + формат отчёта, tests files по `find forge-plugin/tests -type f | wc -l`); decisions.yml — запись status-report-phase-5 (в decision: stale_tasks, режимы render/merged/link/summary); journal.yml — запись 2026-09-04 первой; status.yml :2 «7-фазный pipeline: unblocker → … → status-report» + строка про отчёт. Как проверим: `grep -h '"version"\|phase' forge-plugin/.claude-plugin/plugin.json forge-plugin/.claude-plugin/marketplace.json .claude-plugin/marketplace.json | sort -u` → только 7.7.0 / 7-phase; `for f in …; do python3 -c "import json,sys; json.load(open(sys.argv[1]))" $f; done` без ошибок; `wc -c .forge/index.yml` ≤ 2400; `ruby -ryaml -e '%w[.forge/index.yml .forge/decisions.yml .forge/journal.yml .forge/map.yml .forge/status.yml].each{|f| YAML.load_file(f)}; puts "YAML OK"'`; `grep -c '^  - id:' .forge/decisions.yml` → 11; `bash forge-plugin/hooks/session-start.sh </dev/null | grep -o 'v7.7.0'` → v7.7.0.
18. [step] (renderer) Шаг 15. Полный прогон bash-тестов и свип по перечислениям
   `cd /Users/mac/Projects/Plugin/plugin && for t in forge-plugin/tests/hooks/test-*.sh; do printf '%s: ' "$t"; bash "$t" | tail -1; done` (6 сьютов: bash-safety, context-inject, memory-backup, session-start, status-report, user-rules-check); `grep -rn '6-фазн\|6-phase\|6 фаз\|ROADMAP' CLAUDE.md GUIDE.md forge-plugin/ .forge/index.yml .forge/status.yml --include=*.md --include=*.yml --include=*.sh --include=*.json | grep -v 'не заводится\|не пишет\|писать ROADMAP.md\|не пишется'`; `git status --short | grep -E '^\?\? (\.playwright-mcp|.*\.png)' || echo clean`. Как проверим: каждая строка цикла — «All tests passed»; grep → пусто; «clean»; в `git status --short` есть `.forge/sketches/status-report-mockup.html`, нет `.forge/status-report.json/.html`.
19. [checkpoint] (docs) Чекпоинт C: «мержим» → релиз 7.7.0 → обновление плагина → вернуть WIP
   Показываем владельцу: список файлов из `git status --short` (только файлы задачи + память; settings.json и forge-tray — в stash) и итог тестов. Владелец говорит «мержим» → finishing-a-development-branch Option 1 (коммит, merge в master, index.yml, backup.sh → push master). Дальше по R1: `git rev-parse master origin/master` → одинаково; `gh api repos/anton-ai5010/forge/contents/forge-plugin/.claude-plugin/plugin.json --jq .content | base64 -d | grep '"version"'` → "7.7.0"; в Claude Code `/plugin marketplace update forge-marketplace` + `/plugin update forge@forge-marketplace`, перезапуск; `python3 -c "import json;print(json.load(open('/Users/mac/.claude/plugins/installed_plugins.json'))['plugins']['forge@forge-marketplace'][0]['version'])"` → 7.7.0; интро новой сессии — «Forge plugin (v7.7.0) активен.» и строка `Phase 5   /forge:status-report`. Затем `git stash pop` (на master или после `git checkout feat/tray-save-command`) → `git stash list` пусто, `git status --short` → ` M .claude/settings.json`, ` M forge-tray/forge-tray-mac.py`. Владелец подтверждает: версия 7.7.0 видна в интро, чужие правки трея вернулись.

### OPEN QUESTIONS FOR OWNER
- Заголовок блока решений в отчёте — «Решения — нужны от тебя» (без имени второго человека, в эталоне было «…и Влада»). Оставляем так на первую версию? Если в каких-то проектах решения принимаются вдвоём — скажи, добавим имя позже.
- Для проверки на живом проекте (чекпоинт B): в каком проекте пробуем и будет ли у него запущен интерфейс в браузере? Без запущенного интерфейса снимки экрана до релиза не проверим — только текстовый отчёт.
- После «мержим» задачи, которой нет в отчёте (взята не из карточки), счётчик «отчёт устарел на N задач» всё равно растёт на 1 — так и оставить (любая влитая задача = цифры могли поплыть)?
